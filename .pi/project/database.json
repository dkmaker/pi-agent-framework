{
  "version": 9,
  "epics": [
    {
      "id": "vp84p1ys",
      "title": "Spike: Pi SDK & RPC Capabilities Discovery",
      "description": "Test the actual capabilities of pi's SDK (`createAgentSession`) and RPC mode to determine what's feasible for the agent framework architecture. These findings gate all design decisions.",
      "body": "",
      "priority": 1,
      "status": "closed",
      "relevantFiles": [],
      "todos": [
        {
          "text": "Spike: SDK createAgentSession — can we load extensions, packages, skills?",
          "done": true
        },
        {
          "text": "Spike: SDK session — can we restart/reload/compact programmatically?",
          "done": true
        },
        {
          "text": "Spike: SDK session — can we stream output and inject messages?",
          "done": true
        },
        {
          "text": "Spike: RPC mode — test headless agent control via JSON protocol",
          "done": true
        },
        {
          "text": "Spike: RPC mode — can we reconnect interactive session to an RPC process?",
          "done": true
        },
        {
          "text": "Spike: Manager self-restart — test alternatives to tmux /new injection",
          "done": true
        },
        {
          "text": "Document findings and update architecture decisions",
          "done": true
        }
      ],
      "successCriteria": [
        "Clear YES/NO answers for each SDK and RPC capability",
        "Working proof-of-concept scripts for each viable path",
        "Architecture decision document: which spawn mode for agents, which for manager"
      ],
      "research": [],
      "createdAt": "2026-03-15T21:18:15.903Z",
      "updatedAt": "2026-03-15T23:06:14.912Z",
      "closeMessage": "All spikes complete. SDK is the runtime, Unix socket is the protocol, tmux for manager. 12 pattern assets + 2 architecture assets created.",
      "closeReason": "done",
      "validations": [
        {
          "criterion": "Clear YES/NO answers for each SDK and RPC capability",
          "evidence": "All SDK capabilities tested with YES/NO in 4 spike issues. 12 sdk-patterns assets document every finding.",
          "met": true
        },
        {
          "criterion": "Working proof-of-concept scripts for each viable path",
          "evidence": "Working spike code in spike-sdk/, spike-service/, spike-queue/, spike-lifecycle/ — all tested and passing.",
          "met": true
        },
        {
          "criterion": "Architecture decision document: which spawn mode for agents, which for manager",
          "evidence": "Architecture decided: SDK for agents, tmux for manager restart, Unix socket for service communication. Documented in architecture assets.",
          "met": true
        }
      ],
      "closedAt": "2026-03-15T23:06:14.912Z"
    },
    {
      "id": "iduwan11",
      "title": "Requirements: Complete Tool & API Mapping",
      "description": "Map all requirements from the requirements asset into concrete tool definitions, API surfaces, configuration schemas, and data structures. This is the design phase before implementation.",
      "body": "",
      "priority": 2,
      "status": "closed",
      "relevantFiles": [],
      "todos": [
        {
          "text": "Map all manager extension tools (names, params, responses)",
          "done": true
        },
        {
          "text": "Map all agent tools (send_message, subscribe_events, etc.)",
          "done": true
        },
        {
          "text": "Design settings.json schema with full validation",
          "done": true
        },
        {
          "text": "Design service API surface (AgentManager methods)",
          "done": true
        },
        {
          "text": "Design protocol commands/events for Unix socket adapter",
          "done": true
        },
        {
          "text": "Map agent state machine transitions and health thresholds",
          "done": true
        },
        {
          "text": "Design trace entry types and query API",
          "done": true
        },
        {
          "text": "Define ACL structure within settings.json",
          "done": true
        },
        {
          "text": "Define per-agent folder structure (.pi/agents/<name>/)",
          "done": true
        },
        {
          "text": "Map event subscription model with bounded delivery",
          "done": true
        }
      ],
      "successCriteria": [
        "Complete tool inventory with parameters and response types",
        "settings.json JSON schema defined",
        "Service API fully specified",
        "All requirements from requirements asset addressed",
        "Ready to create implementation issues from this spec"
      ],
      "research": [
        {
          "type": "comment",
          "content": "**⚠️ Unvalidated assets:** The 5 requirement assets added for service API, protocol, state machine, trace, and event subscriptions are NOT user-validated. Follow them as strong guidance but they can be broken during implementation if needed.",
          "addedAt": "2026-03-15T23:31:39.995Z"
        }
      ],
      "createdAt": "2026-03-15T22:16:31.500Z",
      "updatedAt": "2026-03-16T00:21:53.131Z",
      "closeMessage": "All 10 design todos completed. 13 requirement assets produced covering every aspect of the system: tools, settings, service API, protocol, state machine, health, trace, events, file structure, hooks, and loop mode. Three research issues closed. Implementation epics already created from these specs.",
      "closeReason": "done",
      "validations": [
        {
          "criterion": "Complete tool inventory with parameters and response types",
          "evidence": "Assets 5c5nibh6 (Manager Tools & Modes) and 0l0qkut7 (Agent-Side Tools) define complete tool inventories with params and responses",
          "met": true
        },
        {
          "criterion": "settings.json JSON schema defined",
          "evidence": "Asset n2j36dl7 (Settings & Config Schema) defines full settings.json schema with validation",
          "met": true
        },
        {
          "criterion": "Service API fully specified",
          "evidence": "Asset i5eisc9o (Agent Service API) specifies AgentManager class, architecture, and behaviors; asset 4o3g4qf3 covers Unix socket protocol",
          "met": true
        },
        {
          "criterion": "All requirements from requirements asset addressed",
          "evidence": "13 requirement assets cover all areas: tools, settings, service API, protocol, state machine, trace, events, file structure, hooks, loop mode, system prompts",
          "met": true
        },
        {
          "criterion": "Ready to create implementation issues from this spec",
          "evidence": "Implementation epics nptk1ujw and jyubwwjn already created with 13+15 todos derived from these specs",
          "met": true
        }
      ],
      "closedAt": "2026-03-16T00:21:53.131Z"
    },
    {
      "id": "nptk1ujw",
      "title": "Implementation: Agent Service",
      "description": "Build the TypeScript agent service that manages SDK agent sessions. This is the backend process that spawns agents, routes messages, tracks state, and exposes a transport-agnostic API over Unix socket.",
      "body": "## What We Know\n\nThe full design is captured in 13 requirement assets under epic [iduwan11]. Key references:\n\n- **[i5eisc9o] Agent Service API** — AgentManager class, methods, ManagedAgent struct, internal architecture\n- **[4o3g4qf3] Unix Socket Protocol** — NDJSON wire format, all commands/events/error codes\n- **[ik1yp2tc] Agent State Machine, Health & Cutoff** — states, transitions, health detection, message delivery triggers, polite/hard cutoff flow\n- **[f5z68c4v] Trace Log & State Recovery** — 13 entry types, query API, crash recovery from trace\n- **[il2m3sl0] Event Subscriptions (Manager-Only)** — bounded delivery, auto-subscriptions, loop prevention\n- **[0l0qkut7] Agent-Side Tools** — 5 tools registered per agent via customTools\n- **[gzoe99o0] System Prompt Assembly** — per-turn rebuild, address book from ACL, unattended instructions\n- **[n2j36dl7] Settings & Config Schema** — full schemas, auto-scaffolding, default merging\n- **[a3m0qqbu] Reference File Structure** — service source layout, all files and who creates them\n- **[0osnjjl4] Extension Hooks & Lifecycle** — SDK session hooks, tool execute handlers, health timer, settings watcher\n\n## Key Architecture Decisions\n\n- Standalone process, survives manager restart\n- SDK `createAgentSession` for all agents, in-process (not tmux)\n- `systemPromptOverride` is a function — rebuilt per turn (live SYSTEM.md, live ACL)\n- `fs.watch()` on settings.json for live config/ACL updates\n- Messages trigger agent turns (`session.prompt()` when idle)\n- Agents are unattended, message-driven workers — no subscriptions\n- Subscriptions are manager-only, delivered via socket events\n- Auto-scaffolding: creates .pi/agents/ + settings.json + all files with defaults on first run\n- Missing fields merged into existing files on every load\n\n## Implementation Scope\n\nThe service is a standalone npm package (`pi-agent-service`) with:\n- CLI entry point (`pi-agent-service --project /path`)\n- AgentManager core class\n- MessageRouter (ACL, queue, threads, delivery)\n- TraceWriter (append-only JSONL)\n- StateRecovery (rebuild from trace)\n- CutoffMonitor + HealthMonitor\n- SubscriptionManager (manager-only)\n- SettingsLoader (read + watch)\n- Agent tools factory (5 customTools per agent)\n- Prompt builder (system prompt assembly function)\n- Unix socket adapter (NDJSON protocol)",
      "priority": 3,
      "status": "in-progress",
      "relevantFiles": [],
      "todos": [
        {
          "text": "Core AgentManager class (transport-agnostic API)",
          "done": false
        },
        {
          "text": "MessageQueue with threading, priority, delivery tracking",
          "done": false
        },
        {
          "text": "Trace — append-only JSONL audit log",
          "done": false
        },
        {
          "text": "Agent state machine (offline/idle/working) with health monitoring",
          "done": false
        },
        {
          "text": "Context cutoff — two-level (polite/hard), never interrupt tool calls",
          "done": false
        },
        {
          "text": "context_handoff tool + session.newSession() + continue flow",
          "done": false
        },
        {
          "text": "Agent-side tools: send_message, context_handoff, list_messages, list_threads, check_status",
          "done": false
        },
        {
          "text": "System prompt assembly (SYSTEM.md + coding guidelines + unattended instructions)",
          "done": false
        },
        {
          "text": "Unix socket transport adapter (JSONL protocol)",
          "done": false
        },
        {
          "text": "Service lifecycle: PID file, singleton, graceful shutdown, state recovery from trace",
          "done": false
        },
        {
          "text": "Default subscriptions: agent_message, agent_state_change",
          "done": false
        },
        {
          "text": "Event subscription system (file_modified, json_value_change, log_error)",
          "done": false
        },
        {
          "text": "settings.json loading and per-agent config resolution",
          "done": false
        }
      ],
      "successCriteria": [
        "Service spawns agents with full isolation (empty agentDir, explicit extensions)",
        "Messages route correctly: agent→agent, agent→manager, manager→agent",
        "Context cutoff handoff works end-to-end (polite + hard, never interrupts tool calls)",
        "Trace log captures all events, replayable for state recovery",
        "Service survives manager restart, flushes queued messages on reconnect",
        "All agent-side tools work (send_message, handoff, list_messages, check_status)"
      ],
      "research": [],
      "createdAt": "2026-03-15T21:18:24.522Z",
      "updatedAt": "2026-03-16T00:22:28.256Z"
    },
    {
      "id": "jyubwwjn",
      "title": "Implementation: Manager Extension",
      "description": "Build the pi extension loaded by the manager's pi session. Connects to the agent service via Unix socket, registers tools grouped by mode, manages subscriptions, status bar, and context cutoff for the manager itself.",
      "body": "## What We Know\n\nThe full design is captured in 13 requirement assets under epic [iduwan11]. Key references:\n\n- **[5c5nibh6] Manager Tools & Modes** — 5 modes, ~40 tools, mode-specific prompt injection, status bar\n- **[8ikfn8hs] Manager Loop Mode** — cycle lifecycle, keep-alive hook, countdown, wake-on-event, LOOP_INSTRUCTIONS.md, DEFERRED.md\n- **[0osnjjl4] Extension Hooks & Lifecycle** — session_start (10-step init), before_agent_start (prompt rebuild), agent_end (loop keep-alive), socket event listeners\n- **[il2m3sl0] Event Subscriptions (Manager-Only)** — auto-subscriptions, bounded delivery\n- **[gzoe99o0] System Prompt Assembly** — manager prompt: SYSTEM.md + mode instructions + address book\n- **[a3m0qqbu] Reference File Structure** — extension source layout, all files\n\n## Key Architecture Decisions\n\n- Pi extension loaded into user's normal TUI session\n- Connects to agent service via Unix socket (auto-starts service if needed)\n- Tools are thin wrappers → validate params → send protocol command → format response\n- Mode switching dynamically enables/disables tools\n- System prompt rebuilt every turn via `before_agent_start`\n- Loop mode has keep-alive hook preventing idle without `manager_loop_next`\n- Auto-scaffolding: creates .pi/agents/ + settings.json + LOOP_INSTRUCTIONS.md + DEFERRED.md\n- manager-state.json persists mode + loop state + subscriptions across context restarts",
      "priority": 4,
      "status": "draft",
      "relevantFiles": [],
      "todos": [
        {
          "text": "Extension entry point: connect to service, auto-start if needed",
          "done": false
        },
        {
          "text": "Mode system: setup/operate/loop/configure/debug with persisted state",
          "done": false
        },
        {
          "text": "Dynamic tool registration/deregistration on mode switch",
          "done": false
        },
        {
          "text": "Mode-specific system prompt injection via before_agent_start",
          "done": false
        },
        {
          "text": "Operate tools: agent_spawn, agent_stop, agent_restart, agent_message, etc.",
          "done": false
        },
        {
          "text": "Configure tools: config_show/set, acl_show/set, agent_system_prompt_edit, etc.",
          "done": false
        },
        {
          "text": "Debug tools: agent_trace, agent_inspect, deferred_list, etc.",
          "done": false
        },
        {
          "text": "Loop tools: manager_monitor, manager_loop_next, manager_defer",
          "done": false
        },
        {
          "text": "Subscription tools: manager_subscribe/unsubscribe/subscriptions",
          "done": false
        },
        {
          "text": "Default subscriptions (agent_message, agent_state_change) with session-only mute",
          "done": false
        },
        {
          "text": "Status bar: mode icon + agent summary + service state",
          "done": false
        },
        {
          "text": "tmux warning if TMUX env not set",
          "done": false
        },
        {
          "text": "Manager context cutoff: polite/hard via before_agent_start, tmux /new injection",
          "done": false
        },
        {
          "text": "Reconnect on context restart: flush messages, restore mode/subscriptions/monitor",
          "done": false
        },
        {
          "text": "Loop wake-on-event: cancel countdown on subscribed event fire",
          "done": false
        }
      ],
      "successCriteria": [
        "Mode switching works with correct tool enable/disable + prompt injection",
        "Status bar shows current mode, agent count, service state",
        "Manager receives agent messages via subscription + sendUserMessage",
        "Loop mode runs unattended with countdown, wakes on events",
        "Manager survives context restart: reconnects, restores mode/state",
        "All 52 tools registered and working across appropriate modes"
      ],
      "research": [],
      "createdAt": "2026-03-15T21:18:30.826Z",
      "updatedAt": "2026-03-16T00:19:40.599Z"
    },
    {
      "id": "p09o4o3f",
      "title": "User Tools: Agent Tailing & Terminal Observation",
      "description": "User-facing tools for observing agents in real-time — tailing agent output in separate terminals, watching live token streams, etc. These are NOT manager tools — they're for the human operator to attach to and watch agents directly.",
      "body": "",
      "priority": 5,
      "status": "draft",
      "relevantFiles": [],
      "todos": [
        {
          "text": "Design CLI/TUI for tailing agent output in separate terminal",
          "done": false
        },
        {
          "text": "Support tailing multiple agents simultaneously",
          "done": false
        },
        {
          "text": "Consider subscription-based tail that highlights events (errors, messages, state changes)",
          "done": false
        }
      ],
      "successCriteria": [],
      "research": [],
      "createdAt": "2026-03-15T23:38:42.485Z",
      "updatedAt": "2026-03-15T23:38:42.485Z"
    }
  ],
  "issues": [
    {
      "id": "otv4gdyn",
      "type": "spike",
      "title": "Spike: SDK createAgentSession capabilities",
      "description": "Test what `createAgentSession` from `@mariozechner/pi-coding-agent` SDK can actually do. **If SDK works well, it eliminates the need for PI_AGENT_NAME env var entirely** — the service manages identity.\\n\\nTest each capability with a small script:\\n1. Can we load custom extensions into an SDK session?\\n2. Can we load packages and skills?\\n3. Can we stream output (token by token)?\\n4. Can we inject user messages programmatically?\\n5. Can we trigger reload/new-session/compaction?\\n6. Can we read the full conversation context?\\n7. What happens on context cutoff — can we catch and handle it?\\n8. Can multiple SDK sessions run in one Node process?\\n9. Can we set/override system prompt per session?\\n10. Can we control tool availability per session?\\n\\nDocument YES/NO with code samples for each.",
      "status": "closed",
      "epicId": "vp84p1ys",
      "linkedIssueIds": [],
      "questions": [],
      "research": [
        {
          "type": "reference",
          "content": "## SDK Capabilities (from docs)\n\n**All critical capabilities confirmed in API:**\n- ✅ `createAgentSession` with custom tools, extensions, system prompt override\n- ✅ `DefaultResourceLoader` with `additionalExtensionPaths` and `extensionFactories`\n- ✅ `systemPromptOverride` — can set per-agent personality\n- ✅ `session.prompt()` / `session.followUp()` — inject messages programmatically\n- ✅ `session.subscribe()` — full event streaming (text deltas, tool calls, lifecycle)\n- ✅ `session.newSession()` — context restart programmatically\n- ✅ `session.compact()` — manual compaction trigger\n- ✅ `SessionManager.inMemory()` — no filesystem session needed\n- ✅ `SettingsManager.inMemory()` — full config control\n- ✅ Custom tools via `customTools: ToolDefinition[]`\n- ✅ `createEventBus()` — inter-extension communication from outside\n\n**This means:** We can spawn multiple `AgentSession` instances in one Node process, each with different system prompts, tools, and settings. No env vars needed. The service IS the orchestrator.",
          "addedAt": "2026-03-15T21:21:13.633Z"
        },
        {
          "type": "example",
          "content": "## Spike Result: ALL CRITICAL CAPABILITIES CONFIRMED ✅\n\nRan `spike-sdk/main.ts` — two agents (Alice & Bob) exchanged 5+ rounds of messages successfully.\n\n### Confirmed capabilities:\n1. ✅ **Multiple AgentSessions in one process** — Alice and Bob ran concurrently\n2. ✅ **Different system prompts per session** — `systemPromptOverride` works perfectly\n3. ✅ **Custom tools per session** — each agent got personalized send_message/check_messages tools\n4. ✅ **Programmatic prompting** — `session.prompt()` drives each agent turn\n5. ✅ **Event subscription** — `session.subscribe()` streams all lifecycle events\n6. ✅ **No coding tools needed** — `tools: []` works, agents only had custom tools\n7. ✅ **In-memory sessions** — `SessionManager.inMemory()` works, no filesystem sessions\n8. ✅ **Filesystem mailbox** — read-once messaging works perfectly\n9. ✅ **Clean disposal** — `session.dispose()` cleans up\n\n### Key observation:\n- Bob was supposed to be \"grumpy\" but the system prompt personality was somewhat ignored — both agents were quite friendly. This is a prompt engineering issue, not an SDK limitation.\n- Total: 28 Alice turns, 30 Bob turns — the SDK managed all state cleanly.\n\n### Architecture implication:\n**No need for PI_AGENT_NAME, tmux, RPC, or launch.sh for agent spawning.** A single TypeScript service using the SDK can manage all agents programmatically. The manager extension just needs tools to interact with this service.",
          "addedAt": "2026-03-15T21:24:32.064Z"
        },
        {
          "type": "comment",
          "content": "## Complete SDK Capabilities Findings\n\n### 1. Multiple Sessions in One Process ✅\nTwo `AgentSession` instances (Alice & Bob) ran concurrently via `Promise.all`. No conflicts, no shared state issues. Each session is fully independent.\n\n### 2. Custom System Prompts per Session ✅\n`systemPromptOverride` in `DefaultResourceLoader` or directly in `createAgentSession` options. Each agent gets a unique personality/role.\n\n### 3. Custom Tools per Session ✅\n`customTools: ToolDefinition[]` — each session gets its own tool set. Agents can have completely different capabilities. `tools: []` removes all built-in coding tools.\n\n### 4. Extension Loading ✅\n`DefaultResourceLoader` with `additionalExtensionPaths` loads real extensions (tested with web-search). Extensions register tools normally via `pi.registerTool()` and they work.\n\n### 5. Programmatic Prompting ✅\n- `session.prompt(text)` — send and wait for completion\n- `session.steer(text)` — interrupt: delivered after current tool\n- `session.followUp(text)` — queued: delivered when agent finishes\n\n### 6. Fully Async / Parallel ✅\n`Promise.all([agentA.prompt(...), agentB.prompt(...)])` works. Agents run truly simultaneously. Filesystem mailbox handles concurrent reads/writes.\n\n### 7. Token Streaming ✅\n`session.subscribe()` fires `message_update` events with `text_delta` for every token. Timestamps allow measuring token gaps for stuck-agent detection. Events:\n- `turn_start` / `turn_end` — per LLM response\n- `tool_execution_start` / `tool_execution_end` — per tool call\n- `agent_start` / `agent_end` — per full prompt cycle\n- `message_start` / `message_end` — per message\n\n### 8. Context Usage ✅\n`session.getContextUsage()` returns:\n```json\n{ \"tokens\": 4645, \"contextWindow\": 1000000, \"percent\": 0.46 }\n```\nReal-time context window percentage — perfect for cutoff detection.\n\n### 9. Session Stats ✅\n`session.getSessionStats()` returns:\n```json\n{\n  \"userMessages\": 2, \"assistantMessages\": 2,\n  \"toolCalls\": 0, \"toolResults\": 0, \"totalMessages\": 4,\n  \"tokens\": { \"input\": 6, \"output\": 17, \"cacheRead\": 9226, \"total\": 9275 },\n  \"cost\": 0.00523\n}\n```\n\n### 10. Per-Message Usage ✅\nEach assistant message has `usage` field:\n```json\n{ \"input\": 3, \"output\": 5, \"cacheRead\": 0, \"cacheWrite\": 4622, \"totalTokens\": 4630,\n  \"cost\": { \"input\": 0.000015, \"output\": 0.000125, \"total\": 0.029 } }\n```\n\n### 11. Session State ✅\n- `session.isStreaming` — boolean, true during generation\n- `session.state` — state object\n- `session.messages` — full conversation history\n\n### 12. Session Management ✅\n- `session.newSession()` — context restart programmatically\n- `session.compact()` — manual compaction\n- `SessionManager.inMemory()` — no filesystem persistence needed\n- `SessionManager.create(cwd)` — persistent sessions available too\n\n### 13. Stuck Agent Detection ✅\nAchievable by monitoring:\n- Time since last `text_delta` event while `isStreaming === true`\n- Turn duration (`turn_start` to `turn_end`)\n- Tool execution duration (`tool_execution_start` to `tool_execution_end`)\n- No `agent_end` received after prolonged period\n- `session.abort()` available to kill a stuck agent\n\n### 14. Settings Control ✅\n`SettingsManager.inMemory({...})` — full control over compaction, retry, etc. per agent.\n\n### Architecture Conclusion\n**The SDK is the clear winner for agent runtime.** It provides:\n- Full programmatic control (no tmux/RPC needed for agents)\n- Real-time observability (tokens, context %, cost, turn count)\n- Health monitoring (streaming state, token gaps, stuck detection)\n- Extension support (load any pi extension into any agent)\n- Clean lifecycle (dispose, restart, compact)\n\n**Remaining question:** Manager self-restart — separate spike (zebwque8).",
          "addedAt": "2026-03-15T21:34:19.350Z"
        },
        {
          "type": "comment",
          "content": "## Additional Findings\n\n### 15. Fully Async / Parallel Agents ✅\n`Promise.all([agentA.prompt(...), agentB.prompt(...)])` — multiple agents run truly simultaneously in one Node process. Tested with Alice & Bob exchanging messages concurrently. See `spike-sdk/main-async.ts`.\n\n### 16. Extension Loading (web-search) ✅\n`additionalExtensionPaths: [\"/path/to/web-search.ts\"]` loads real extensions into SDK sessions. Bob used `web_search` tool to answer Alice's question. See `spike-sdk/main-websearch.ts`.\n\n### 17. Full Isolation ✅\nEmpty `agentDir` + empty `cwd` = zero discovery. No global extensions, skills, prompts, or themes. Only explicitly provided tools/extensions load. See `spike-sdk/inspect-isolation.ts`.\n\n### 18. Selective Extension Loading with Project Access ✅\n`cwd` = project root (for `.pi/` database access), `agentDir` = empty dir (blocks global extensions), `additionalExtensionPaths` = explicit list. Tested: PM extension loaded alone, agent called `epic_list` + `issue_list` successfully against real project database. See `spike-sdk/inspect-pm-test.ts`.\n\n### 19. Persistent Sessions / Transcript ✅\n`SessionManager.create(cwd)` writes full JSONL transcript to disk. Every message, model change, thinking level change persisted with tree structure (id/parentId). `SessionManager.inMemory()` available for no-persistence mode. See `spike-sdk/inspect-sessions.ts`.",
          "addedAt": "2026-03-15T21:46:09.819Z"
        }
      ],
      "createdAt": "2026-03-15T21:18:50.346Z",
      "updatedAt": "2026-03-15T21:53:07.421Z",
      "autoValidation": {
        "type": "human",
        "strategy": "User reviews findings and confirms they answer the architecture questions"
      },
      "closeMessage": "SDK spike complete. Architecture decision: SDK for agents, tmux for manager self-restart. All patterns documented in sdk-patterns assets.",
      "closeReason": "done",
      "validations": [
        {
          "criterion": "Test what `createAgentSession` from `@mariozechner/pi-coding-agent` SDK can actually do. **If SDK works well, it eliminates the need for PI_AGENT_NAME env var entirely** — the service manages identity.\\n\\nTest each capability with a small script:\\n1. Can we load custom extensions into an SDK session?\\n2. Can we load packages and skills?\\n3. Can we stream output (token by token)?\\n4. Can we inject user messages programmatically?\\n5. Can we trigger reload/new-session/compaction?\\n6. Can we read the full conversation context?\\n7. What happens on context cutoff — can we catch and handle it?\\n8. Can multiple SDK sessions run in one Node process?\\n9. Can we set/override system prompt per session?\\n10. Can we control tool availability per session?\\n\\nDocument YES/NO with code samples for each.",
          "evidence": "User confirmed \"Yes thats fine close it\". All 10 SDK capabilities tested with working code. 8 assets created with source references. SDK confirmed as primary agent runtime.",
          "met": true
        }
      ],
      "closedAt": "2026-03-15T21:53:07.421Z"
    },
    {
      "id": "r7ivt3j9",
      "type": "spike",
      "title": "Spike: RPC mode for headless agent control",
      "description": "**DEPRIORITIZED** — SDK spike confirmed SDK is the primary runtime. RPC remains a reference for manager self-restart scenarios only. The manager ↔ service communication spike (44uom9qq) supersedes most RPC questions. May revisit if crash isolation becomes a requirement.",
      "status": "closed",
      "epicId": "vp84p1ys",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-15T21:18:57.298Z",
      "updatedAt": "2026-03-15T23:05:46.681Z",
      "autoValidation": {
        "type": "human",
        "strategy": "User reviews findings and confirms viability vs SDK approach"
      },
      "closeMessage": "Superseded by SDK. Not needed.",
      "closeReason": "wont-fix",
      "validations": [
        {
          "criterion": "**DEPRIORITIZED** — SDK spike confirmed SDK is the primary runtime. RPC remains a reference for manager self-restart scenarios only. The manager ↔ service communication spike (44uom9qq) supersedes most RPC questions. May revisit if crash isolation becomes a requirement.",
          "evidence": "User selected \"Close both — decisions are made (SDK + tmux)\" in questionnaire. SDK is the runtime, RPC not needed.",
          "met": false
        }
      ],
      "closedAt": "2026-03-15T23:05:46.681Z"
    },
    {
      "id": "zebwque8",
      "type": "spike",
      "title": "Spike: Manager self-restart without tmux",
      "description": "Manager runs in tmux for self-restart via `/new` injection — same as today. This is the simplest proven approach. Can revisit once the agent service is built (service could potentially respawn the manager). Low priority — solve after the service communication spike.",
      "status": "closed",
      "epicId": "vp84p1ys",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-15T21:19:05.828Z",
      "updatedAt": "2026-03-15T23:06:04.685Z",
      "autoValidation": {
        "type": "human",
        "strategy": "User confirms a viable manager restart strategy exists"
      },
      "closeMessage": "Not needed as a spike — decision already made: tmux for manager restart.",
      "closeReason": "wont-fix",
      "validations": [
        {
          "criterion": "Manager runs in tmux for self-restart via `/new` injection — same as today. This is the simplest proven approach. Can revisit once the agent service is built (service could potentially respawn the manager). Low priority — solve after the service communication spike.",
          "evidence": "User chose \"Close both — decisions are made (SDK + tmux)\" in questionnaire. No alternative to tmux needed — manager runs in tmux, extension warns otherwise.",
          "met": false
        }
      ],
      "closedAt": "2026-03-15T23:06:04.685Z"
    },
    {
      "id": "44uom9qq",
      "type": "spike",
      "title": "Spike: Manager Extension ↔ Agent Service communication protocol",
      "description": "The architecture is now: **manager pi session** (runs in tmux, has an extension with tools) talks to an **agent service** (TypeScript process using SDK to manage agent sessions). We need to spike the communication layer.\n\n## Key Questions\n\n### 1. Protocol\n- WebSocket? Unix socket? HTTP? Filesystem events? stdin/stdout pipe?\n- What gives us real-time bidirectional streaming + simplicity?\n- Can the manager extension spawn and connect to the service?\n\n### 2. Manager → Service (commands)\n- `spawn_agent(name, config)` — start an agent\n- `send_message(agent, content)` — deliver a message to an agent\n- `get_status(agent)` — context %, tokens, streaming state\n- `stop_agent(name)` — kill an agent\n- `restart_agent(name)` — context reset\n\n### 3. Service → Manager (events)\n- Agent status changes (idle, streaming, stuck)\n- Messages FROM agents TO manager (the inbox)\n- Token streaming / output tailing\n- Context cutoff warnings\n- Agent completion / error events\n\n### 4. Message Flow\n- How do agents send messages? Custom tool in their session that calls back to the service?\n- How does manager receive messages? Events pushed via the protocol?\n- Filesystem mailbox still for persistence / audit trail?\n- Service as the message router (not direct agent-to-agent filesystem)?\n\n### 5. Future-proofing\n- Manager may later become an agent inside the service too\n- Protocol should work for both external client (pi extension) and internal (agent-to-agent)\n\n## Deliverable\nA working mini-system:\n1. A small TypeScript service that manages 2 SDK agent sessions\n2. A pi extension that connects to the service and exposes tools\n3. Agents exchange messages through the service\n4. Manager sees real-time status and message flow",
      "status": "closed",
      "epicId": "vp84p1ys",
      "linkedIssueIds": [],
      "questions": [],
      "research": [
        {
          "type": "comment",
          "content": "## Protocol Decision: Unix Socket\n\n**Why Unix socket:**\n- No port conflicts (file-based, e.g. `/tmp/pi-agents-<project>.sock`)\n- Low latency, bidirectional streaming\n- Natural cleanup (delete file on exit)\n- Works perfectly for local-only service\n\n**Queue/buffer needs:**\n- Manager may be mid-turn when agent messages arrive → buffer in service, deliver when polled\n- Multiple agents may fire events simultaneously → service serializes\n- Manager extension polls or gets notified via the socket connection\n\n**Protocol format:** JSON lines over Unix socket (same pattern as RPC mode — proven to work)\n\n**Message flow design:**\n1. Service listens on Unix socket\n2. Manager extension connects on load\n3. Extension sends commands (spawn, message, status)\n4. Service pushes events (agent messages, status changes, errors)\n5. Extension injects received messages into manager's conversation via `pi.sendUserMessage()` or similar",
          "addedAt": "2026-03-15T21:54:39.274Z"
        },
        {
          "type": "example",
          "content": "## Spike Result: Unix Socket Protocol WORKS ✅\n\nBuilt and tested a complete mini-system in `spike-service/`:\n\n### Components\n1. **`protocol.ts`** — Shared types (Command/ServiceEvent), JSONL encode/parse helpers, socket path\n2. **`service.ts`** — Agent service: listens on Unix socket, manages SDK sessions, routes messages\n3. **`extension/index.ts`** — Pi extension: connects to service, registers tools (agent_spawn, agent_message, agent_status, agent_stop, agent_tail)\n4. **`test-client.ts`** — Standalone test client proving the protocol\n\n### Proven Flow\n1. ✅ Service listens on `/tmp/pi-agent-service.sock`\n2. ✅ Client connects, receives `service_ready`\n3. ✅ `spawn_agent` → service creates SDK session → returns `agent_spawned`\n4. ✅ `send_message` → service routes to agent via `session.prompt()` or `session.followUp()`\n5. ✅ Agent uses `send_message` tool → service detects target is \"manager\" → pushes `agent_message` event to client\n6. ✅ `get_status` → returns per-agent context %, tokens, cost, streaming state, message counts\n7. ✅ Extension uses `pi.sendUserMessage()` to inject agent messages into manager conversation\n\n### Key Decisions Validated\n- **JSONL over Unix socket** — simple, fast, no dependencies\n- **Service as message router** — agents don't talk directly, service mediates\n- **Filesystem log for persistence** — `messages.log` as audit trail\n- **`prompt()` vs `followUp()`** — use `prompt()` when agent is idle, `followUp()` when streaming\n\n### Minor Issues Found\n- `knownAgents` list built at spawn time — needs to be dynamic (agent spawned later not in list)\n- Need to handle agent-to-agent messaging (currently only agent→manager tested)",
          "addedAt": "2026-03-15T21:58:37.106Z"
        }
      ],
      "createdAt": "2026-03-15T21:53:25.025Z",
      "updatedAt": "2026-03-15T22:08:01.594Z",
      "autoValidation": {
        "type": "human",
        "strategy": "User confirms the protocol choice and working demo answer the architecture questions"
      },
      "closeMessage": "Protocol spike complete.",
      "closeReason": "done",
      "validations": [
        {
          "criterion": "The architecture is now: **manager pi session** (runs in tmux, has an extension with tools) talks to an **agent service** (TypeScript process using SDK to manage agent sessions). We need to spike the communication layer.\n\n## Key Questions\n\n### 1. Protocol\n- WebSocket? Unix socket? HTTP? Filesystem events? stdin/stdout pipe?\n- What gives us real-time bidirectional streaming + simplicity?\n- Can the manager extension spawn and connect to the service?\n\n### 2. Manager → Service (commands)\n- `spawn_agent(name, config)` — start an agent\n- `send_message(agent, content)` — deliver a message to an agent\n- `get_status(agent)` — context %, tokens, streaming state\n- `stop_agent(name)` — kill an agent\n- `restart_agent(name)` — context reset\n\n### 3. Service → Manager (events)\n- Agent status changes (idle, streaming, stuck)\n- Messages FROM agents TO manager (the inbox)\n- Token streaming / output tailing\n- Context cutoff warnings\n- Agent completion / error events\n\n### 4. Message Flow\n- How do agents send messages? Custom tool in their session that calls back to the service?\n- How does manager receive messages? Events pushed via the protocol?\n- Filesystem mailbox still for persistence / audit trail?\n- Service as the message router (not direct agent-to-agent filesystem)?\n\n### 5. Future-proofing\n- Manager may later become an agent inside the service too\n- Protocol should work for both external client (pi extension) and internal (agent-to-agent)\n\n## Deliverable\nA working mini-system:\n1. A small TypeScript service that manages 2 SDK agent sessions\n2. A pi extension that connects to the service and exposes tools\n3. Agents exchange messages through the service\n4. Manager sees real-time status and message flow",
          "evidence": "User confirmed twice: \"Perfect\" and \"Yes its perfect as is\" — explicit validation of protocol spike results.",
          "met": true
        }
      ],
      "closedAt": "2026-03-15T22:08:01.594Z"
    },
    {
      "id": "88lkl8eb",
      "type": "spike",
      "title": "Spike: Message Queue, Service Lifecycle & Agent State Design",
      "description": "Design and test the message queue system, service lifecycle management, and agent state tracking. This is a research + prototype spike.\n\n## Questions to Answer\n\n### Service Lifecycle\n- How does the manager extension start the service? (spawn child process?)\n- How to ensure only ONE service instance runs? (PID file? socket lock?)\n- Service must be independent of manager — survives manager restart\n- Manager reconnects to existing service on restart\n- How to detect if service is already running?\n\n### Message Queue\n- Messages persist in the service (not just pass-through)\n- Queue per agent — delivered when agent is ready (idle)\n- Message states: queued → delivered → read → replied\n- Delivery modes:\n  - `persist` (default) — queued if offline, delivered when available\n  - `online-only` — dropped if agent is offline, but still traced/logged\n- ALL messages stored for debugging/audit (even non-queued ones)\n\n### Threads\n- Messages belong to threads\n- Thread = conversation context between two parties\n- Max ~100 messages per thread → auto-instruction to start new thread (loop prevention)\n- Thread metadata: participants, message count, created_at, last_activity\n- Reply-to support — message references parent message ID\n\n### Message Priority\n- `normal` — standard queue order\n- `important` — flagged, may interrupt agent (steer vs followUp)\n- Priority affects delivery order in queue\n\n### Agent States\n- `offline` — not spawned\n- `online-idle` — spawned, waiting for work\n- `online-working` — actively streaming/processing\n- `online-blocked` — waiting for human input or external resource\n- State transitions tracked and logged\n\n### Trace & Audit\n- Every message logged regardless of delivery mode\n- Every state change logged\n- Every tool call logged\n- Queryable history per agent, per thread, per time range\n\n## Deliverable\n- Design document with data structures\n- Working prototype testing: queue persistence, thread management, state tracking\n- Test: manager restarts, reconnects, picks up queued messages",
      "status": "closed",
      "epicId": "vp84p1ys",
      "linkedIssueIds": [],
      "questions": [],
      "research": [
        {
          "type": "reference",
          "content": "## Existing Messaging System Analysis\n\n### What works well (keep)\n- **Thread model** with max 100 messages + loop prevention\n- **ACL** — who can talk to who\n- **Message format** — frontmatter (from, to, type, timestamp, subject, thread_id) + body\n- **Archive on read** — messages move from inbox to archive\n- **steer vs followup** delivery modes based on `important` flag\n- **File locking** for thread state (prevents race conditions)\n\n### What changes with SDK service architecture\n- **No filesystem polling** — service routes messages directly via `session.prompt()`/`session.followUp()`\n- **No per-agent inbox dirs** — service holds message queues in memory + persists to single store\n- **No file-based message format** — messages are objects in the service, persisted as JSONL\n- **ACL moves to settings.json** — single config source\n- **Thread tracker is in-service** — no lockfile needed (single process)\n\n### Key patterns to port\n1. Thread ID generation and tracking\n2. Max 100 messages per thread\n3. steer (important) vs followup (normal) delivery\n4. Message read/archive tracking\n5. Agent brief/description in ACL",
          "addedAt": "2026-03-15T22:09:08.547Z"
        },
        {
          "type": "comment",
          "content": "## Service Lifecycle Design\n\n### Startup Pattern\n1. Manager extension checks if service is running (try connect to socket)\n2. If not running → spawn as detached child process (`child_process.spawn` with `detached: true, stdio: 'ignore'`)\n3. Service writes PID to `/tmp/pi-agent-service.pid` + locks the socket path\n4. Manager extension retries connection until service is ready\n\n### Singleton Guarantee\n- Socket path acts as lock — `net.createServer().listen(path)` fails if already bound\n- PID file for stale detection — if PID file exists but process is dead, clean up and start fresh\n- On startup: check PID file → check if process alive → if dead, remove socket + pid → start\n\n### Manager Reconnect\n- Service is independent of manager lifecycle\n- On manager restart: extension reconnects to existing service\n- Service keeps all state (agents, queues, threads) in memory + persisted to disk\n- On reconnect: manager gets `service_ready` + can query state of all agents + any queued messages for \"manager\"\n\n### Service Shutdown\n- Graceful: SIGTERM → dispose all agents → close socket → remove PID file\n- Crash: PID file left behind → next startup detects stale, cleans up\n- Optional: service auto-shutdown after N minutes of no connected clients\n\n### Service State Persistence\n- `{agentsDir}/service-state.jsonl` — append-only log of:\n  - Agent spawns/stops\n  - All messages (with delivery status)\n  - State transitions\n  - Thread metadata\n- On service restart: replay log to rebuild state",
          "addedAt": "2026-03-15T22:09:21.302Z"
        },
        {
          "type": "comment",
          "content": "## Message Queue Design\n\n### Message Object\n```typescript\ninterface Message {\n  id: string;              // unique ID (uuid or timestamp-based)\n  threadId: string;        // thread this belongs to\n  from: string;            // sender name\n  to: string;              // recipient name\n  subject: string;         // short topic (5-10 words)\n  body: string;            // markdown content\n  priority: \"normal\" | \"important\";\n  delivery: \"persist\" | \"online-only\";\n  replyTo?: string;        // parent message ID (for threading)\n  timestamp: number;       // created at\n  status: \"queued\" | \"delivered\" | \"read\" | \"failed\";\n  deliveredAt?: number;\n  readAt?: number;\n}\n```\n\n### Queue Behavior\n- Each agent has a queue (FIFO, priority-aware)\n- `important` messages jump to front of queue\n- Queue processes when agent is `online-idle`:\n  - Pop message → deliver via `session.prompt()` or `session.steer()`\n  - Mark as `delivered`\n  - Agent's `send_message` tool response marks originating message as `read`\n- `online-only` messages: if agent is offline → mark `failed`, still logged\n- `persist` messages: stay in queue until agent comes online\n\n### Delivery Strategy\n| Priority | Agent State | Action |\n|----------|------------|--------|\n| normal | idle | `session.prompt(msg)` |\n| normal | working | queue, wait for idle |\n| important | idle | `session.prompt(msg)` |\n| important | working | `session.steer(msg)` — interrupts after current tool |\n| any | offline | queue (persist) or fail (online-only) |\n\n### Manager Queue\n- Manager is special — messages TO manager are pushed via socket events\n- If manager is disconnected: messages queue in service\n- On reconnect: service flushes queued manager messages\n\n### Thread Rules\n- Thread auto-created on first message without `replyTo`\n- Thread ID = `{from}-{timestamp}` or explicit\n- Max 100 messages per thread → message rejected with instruction to start new thread\n- Thread metadata: participants, message count, created, last_activity",
          "addedAt": "2026-03-15T22:09:36.652Z"
        },
        {
          "type": "comment",
          "content": "## Agent State Machine\n\n```\n                    spawn\n  [offline] ──────────────→ [online-idle]\n      ↑                        │    ↑\n      │ stop/crash             │    │ agent_end event\n      │                        │    │\n      └────────────────── [online-working]\n                               │    ↑\n                               │    │ prompt delivered\n                               ↓    │\n                          [online-idle]\n```\n\n### States\n- `offline` — not spawned, or disposed\n- `online-idle` — session exists, not streaming, ready for messages\n- `online-working` — session is streaming (isStreaming=true)\n- `online-blocked` — future: waiting for human input (questionnaire tool)\n\n### Transitions (all logged)\n```typescript\ninterface StateTransition {\n  agent: string;\n  from: AgentState;\n  to: AgentState;\n  timestamp: number;\n  reason: string;  // \"spawned\", \"prompt_delivered\", \"turn_complete\", \"stopped\", \"crashed\", \"context_cutoff\"\n}\n```\n\n### Health Monitoring (per agent)\n- `lastActivity` — timestamp of last event\n- `lastTokenTime` — timestamp of last text_delta\n- `turnStartTime` — when current turn started\n- Stuck detection: `online-working` for > N seconds with no tokens → flag\n\n### Status Snapshot (queryable)\n```typescript\ninterface AgentSnapshot {\n  name: string;\n  state: AgentState;\n  contextPercent: number;\n  tokenUsage: { input: number; output: number; total: number };\n  cost: number;\n  messageStats: { sent: number; received: number; queued: number };\n  threadCount: number;\n  uptime: number;\n  lastActivity: number;\n  health: \"healthy\" | \"slow\" | \"stuck\" | \"error\";\n}\n```",
          "addedAt": "2026-03-15T22:09:49.266Z"
        },
        {
          "type": "comment",
          "content": "## Audit Trail / Trace Design\n\n### Single append-only JSONL log: `{agentsDir}/trace.jsonl`\n\nEvery event gets a line:\n```typescript\ntype TraceEntry =\n  | { type: \"message\"; message: Message }\n  | { type: \"state_change\"; transition: StateTransition }\n  | { type: \"agent_spawn\"; name: string; config: AgentConfig; timestamp: number }\n  | { type: \"agent_stop\"; name: string; reason: string; timestamp: number }\n  | { type: \"tool_call\"; agent: string; tool: string; params: any; timestamp: number }\n  | { type: \"delivery\"; messageId: string; status: \"delivered\" | \"read\" | \"failed\"; timestamp: number }\n  | { type: \"thread_created\"; threadId: string; participants: string[]; timestamp: number }\n  | { type: \"thread_exhausted\"; threadId: string; count: number; timestamp: number }\n  | { type: \"service_start\"; timestamp: number }\n  | { type: \"service_stop\"; timestamp: number }\n  | { type: \"client_connect\"; clientType: string; timestamp: number }\n  | { type: \"client_disconnect\"; clientType: string; timestamp: number }\n  | { type: \"error\"; agent?: string; error: string; timestamp: number }\n```\n\n### Properties\n- **Append-only** — never modified, only appended\n- **ALL messages traced** — including `online-only` that weren't delivered\n- **Replayable** — service can rebuild state from trace on restart\n- **Queryable** — tools can filter by agent, thread, time range, type\n- **Rotatable** — future: rotate when file exceeds N MB",
          "addedAt": "2026-03-15T22:09:59.826Z"
        },
        {
          "type": "example",
          "content": "## Spike Result: All Core Patterns Proven ✅\n\nBuilt and tested `spike-queue/` — transport-agnostic AgentManager with:\n- **MessageQueue**: priority, threading, delivery tracking, online-only mode, thread exhaustion at 100\n- **AgentManager**: state machine (offline/idle/working), health detection, queue delivery loop, event subscriptions\n- **Trace**: append-only JSONL audit log — 129 entries in test covering messages, state changes, deliveries, threads\n- All tests passed: queue delivery, agent-to-agent routing, online-only failure, thread exhaustion, state transitions",
          "addedAt": "2026-03-15T22:15:38.850Z"
        }
      ],
      "createdAt": "2026-03-15T22:08:19.157Z",
      "updatedAt": "2026-03-15T22:16:21.557Z",
      "autoValidation": {
        "type": "human",
        "strategy": "User confirms the queue design and service lifecycle patterns answer the requirements"
      },
      "closeMessage": "Spike complete. All core patterns proven. Moving to requirements mapping.",
      "closeReason": "done",
      "validations": [
        {
          "criterion": "Design and test the message queue system, service lifecycle management, and agent state tracking. This is a research + prototype spike.\n\n## Questions to Answer\n\n### Service Lifecycle\n- How does the manager extension start the service? (spawn child process?)\n- How to ensure only ONE service instance runs? (PID file? socket lock?)\n- Service must be independent of manager — survives manager restart\n- Manager reconnects to existing service on restart\n- How to detect if service is already running?\n\n### Message Queue\n- Messages persist in the service (not just pass-through)\n- Queue per agent — delivered when agent is ready (idle)\n- Message states: queued → delivered → read → replied\n- Delivery modes:\n  - `persist` (default) — queued if offline, delivered when available\n  - `online-only` — dropped if agent is offline, but still traced/logged\n- ALL messages stored for debugging/audit (even non-queued ones)\n\n### Threads\n- Messages belong to threads\n- Thread = conversation context between two parties\n- Max ~100 messages per thread → auto-instruction to start new thread (loop prevention)\n- Thread metadata: participants, message count, created_at, last_activity\n- Reply-to support — message references parent message ID\n\n### Message Priority\n- `normal` — standard queue order\n- `important` — flagged, may interrupt agent (steer vs followUp)\n- Priority affects delivery order in queue\n\n### Agent States\n- `offline` — not spawned\n- `online-idle` — spawned, waiting for work\n- `online-working` — actively streaming/processing\n- `online-blocked` — waiting for human input or external resource\n- State transitions tracked and logged\n\n### Trace & Audit\n- Every message logged regardless of delivery mode\n- Every state change logged\n- Every tool call logged\n- Queryable history per agent, per thread, per time range\n\n## Deliverable\n- Design document with data structures\n- Working prototype testing: queue persistence, thread management, state tracking\n- Test: manager restarts, reconnects, picks up queued messages",
          "evidence": "User confirmed \"Yes its solid\". All patterns proven: queue, threading, state machine, trace, health monitoring. Requirements asset created as project-level reference.",
          "met": true
        }
      ],
      "closedAt": "2026-03-15T22:16:21.557Z"
    },
    {
      "id": "zwzjxn50",
      "type": "spike",
      "title": "Spike: Service lifecycle — spawn, survive, reconnect",
      "description": "Test the critical service lifecycle flows:\n\n## Service Start/Stop/Reconnect\n1. **Spawn from extension** — can a pi extension spawn a detached Node process that survives?\n2. **Singleton** — socket lock prevents duplicate services\n3. **Survive manager exit** — kill pi, service keeps running, agents keep working\n4. **Reconnect** — restart pi, extension connects to existing service, gets queued messages\n5. **Graceful shutdown** — SIGTERM cleans up agents, socket, PID file\n6. **Stale detection** — if service crashed, detect stale PID/socket and restart\n\n## Agent Context Cutoff & Handoff\n7. **Context monitoring** — service watches `getContextUsage().percent` per agent\n8. **Steer interrupt** — at threshold, `session.steer()` forces agent to stop current work\n9. **Handoff tool** — agent calls `context_handoff(summary, continueMessage)` to save state\n10. **Session reset** — service calls `session.newSession()` on the same session object\n11. **Continue** — service prompts with continueMessage + system prompt after reset\n12. **Does newSession() keep the session object alive?** Or do we need to create a fresh one?\n\n## Deliverables\n- Working service spawn/reconnect test\n- Working context cutoff → handoff → reset → continue test",
      "status": "closed",
      "epicId": "vp84p1ys",
      "linkedIssueIds": [],
      "questions": [],
      "research": [
        {
          "type": "comment",
          "content": "## Research: Detached Process Spawning in Node.js\n\nStandard pattern for spawning a service that survives parent:\n```typescript\nconst child = spawn('node', ['service.js'], {\n  detached: true,\n  stdio: 'ignore',\n  env: { ...process.env },\n});\nchild.unref(); // parent won't wait for child\n```\n\nFor tsx (TypeScript): `spawn('npx', ['tsx', 'service.ts'], { detached: true, stdio: 'ignore' })`\n\n## Research: session.newSession()\nFrom SDK docs: `newSession(options?: { parentSession?: string }): Promise<boolean>` — returns boolean indicating success. Need to test if the session object remains usable after this call.",
          "addedAt": "2026-03-15T22:20:06.442Z"
        },
        {
          "type": "example",
          "content": "## All Tests Passed ✅\n\n### Service Lifecycle (spike-lifecycle/test-lifecycle.ts)\n- Detached spawn with `child.unref()` — service independent of parent\n- Socket lock = singleton guarantee\n- Service survives parent exit, queues messages while disconnected\n- Reconnect flushes queued messages (3 messages delivered)\n- SIGTERM graceful cleanup (socket + PID file removed)\n- Stale PID detection via `process.kill(pid, 0)`\n\n### Context Cutoff Handoff (spike-lifecycle/test-cutoff2.ts)\n- Agent calls `context_handoff(summary, continueMessage)` on instruction\n- `session.newSession()` returns true, resets to 0 messages/0 tokens\n- Session object stays alive and usable after reset\n- Agent continues with handoff message in fresh context\n- Full cycle: 5753 tokens → 0 → 5494 tokens",
          "addedAt": "2026-03-15T22:24:20.830Z"
        },
        {
          "type": "comment",
          "content": "## System Prompt Finding\n\n- `systemPromptOverride` **fully replaces** pi's default system prompt (only override text + date + cwd remain)\n- Without override, pi injects: coding guidelines, tool listing, doc references (~1553 chars)\n- **Tool schemas are sent via the API**, NOT in the system prompt — the \"Available tools:\" section is redundant for Anthropic\n- `before_agent_start` event can **append** to existing prompt instead of replacing\n- **Decision:** Use override with complete agent prompt (SYSTEM.md + framework instructions). Include relevant pi guidelines in our own templates. No dependency on pi's internal prompt format.",
          "addedAt": "2026-03-15T22:27:34.604Z"
        }
      ],
      "createdAt": "2026-03-15T22:18:07.986Z",
      "updatedAt": "2026-03-15T22:34:50.737Z",
      "autoValidation": {
        "type": "human",
        "strategy": "User confirms service survives pi restart and reconnect works"
      },
      "closeMessage": "Lifecycle spike complete. All patterns proven and documented as assets.",
      "closeReason": "done",
      "validations": [
        {
          "criterion": "Test the critical service lifecycle flows:\n\n## Service Start/Stop/Reconnect\n1. **Spawn from extension** — can a pi extension spawn a detached Node process that survives?\n2. **Singleton** — socket lock prevents duplicate services\n3. **Survive manager exit** — kill pi, service keeps running, agents keep working\n4. **Reconnect** — restart pi, extension connects to existing service, gets queued messages\n5. **Graceful shutdown** — SIGTERM cleans up agents, socket, PID file\n6. **Stale detection** — if service crashed, detect stale PID/socket and restart\n\n## Agent Context Cutoff & Handoff\n7. **Context monitoring** — service watches `getContextUsage().percent` per agent\n8. **Steer interrupt** — at threshold, `session.steer()` forces agent to stop current work\n9. **Handoff tool** — agent calls `context_handoff(summary, continueMessage)` to save state\n10. **Session reset** — service calls `session.newSession()` on the same session object\n11. **Continue** — service prompts with continueMessage + system prompt after reset\n12. **Does newSession() keep the session object alive?** Or do we need to create a fresh one?\n\n## Deliverables\n- Working service spawn/reconnect test\n- Working context cutoff → handoff → reset → continue test",
          "evidence": "User confirmed \"Yes works!\" All lifecycle tests passed: detached spawn, singleton, survive, reconnect, shutdown, cutoff handoff, system prompt strategy.",
          "met": true
        }
      ],
      "closedAt": "2026-03-15T22:34:50.737Z"
    },
    {
      "id": "8fa9ieh0",
      "type": "feature",
      "title": "Define Manager Extension Tool Inventory",
      "description": "Map all tools the manager extension registers, with complete parameter schemas and response formats.\n\nThese are the tools the human operator (or future manager agent) uses to control the agent service.\n\nCategories:\n1. **Agent management** — spawn, stop, restart, list\n2. **Messaging** — send message, view all messages, view threads\n3. **Observability** — status, tail, peek, dashboard\n4. **Service** — start service, stop service, service status\n5. **Configuration** — reload config, view config\n\nFor each tool: name, description, parameters (TypeBox schema), response format, example usage.",
      "status": "closed",
      "epicId": "iduwan11",
      "linkedIssueIds": [],
      "questions": [],
      "research": [
        {
          "type": "comment",
          "content": "## Manager Extension Tool Inventory\n\n### 1. Agent Management\n\n#### `agent_spawn`\nSpawn a new agent session in the service.\n```\nParameters:\n  name: string          — unique agent identifier\n  config_override?: {   — optional overrides (defaults from settings.json)\n    model?: string\n    thinking?: string\n    extensions?: string[]\n    cutoff_polite?: number\n    cutoff_hard?: number\n  }\nResponse: \"Agent {name} spawned\" or error\n```\nNotes: reads `.pi/agents/{name}/SYSTEM.md` for system prompt, merges with settings.json config.\n\n#### `agent_stop`\nStop a running agent.\n```\nParameters:\n  name: string\nResponse: \"Agent {name} stopped\"\n```\n\n#### `agent_restart`\nContext reset — triggers handoff + newSession.\n```\nParameters:\n  name: string\n  continue_message?: string  — optional override (otherwise uses agent's default init prompt)\nResponse: \"Agent {name} restarted with fresh context\"\n```\n\n#### `agent_list`\nList all agents with current state.\n```\nParameters: (none)\nResponse: table of agents with state, context %, health, uptime\n```\n\n---\n\n### 2. Messaging\n\n#### `agent_message`\nSend a message to an agent.\n```\nParameters:\n  to: string              — agent name\n  subject: string         — short topic (5-10 words)\n  message: string         — body content (markdown)\n  important?: boolean     — priority flag (default: false)\n  thread_id?: string      — continue existing thread (omit for new)\nResponse: \"Message sent to {to} [thread: {id}, #{count}]\"\n```\n\n#### `agent_messages`\nView messages — all, per-agent, or per-thread.\n```\nParameters:\n  agent?: string          — filter by agent (sent or received)\n  thread_id?: string      — filter by thread\n  limit?: number          — max messages (default: 20)\n  include_agent_to_agent?: boolean  — include inter-agent msgs (default: true)\nResponse: formatted message list with from, to, subject, status, timestamp\n```\n\n#### `agent_threads`\nList active threads.\n```\nParameters:\n  agent?: string          — filter by participant\n  limit?: number          — default: 10\nResponse: thread list with id, subject, participants, message count, last activity\n```\n\n---\n\n### 3. Observability\n\n#### `agent_status`\nGet status of all agents or one agent.\n```\nParameters:\n  name?: string           — specific agent (omit for all)\nResponse: per-agent: state, context %, tokens, cost, messages sent/recv/queued, health, uptime\n```\n\n#### `agent_tail`\nEnable/disable real-time output streaming for an agent.\n```\nParameters:\n  name: string\n  enabled: boolean\nResponse: \"Tailing enabled/disabled for {name}\"\n```\nNote: text deltas injected as status updates or logged.\n\n#### `agent_peek`\nGet last N lines of agent output (recent conversation).\n```\nParameters:\n  name: string\n  lines?: number          — default: 20\nResponse: last N assistant messages or text fragments\n```\n\n#### `agent_history`\nGet full agent conversation history.\n```\nParameters:\n  name: string\n  limit?: number          — default: 50\nResponse: full message history (user + assistant turns)\n```\n\n---\n\n### 4. Service Management\n\n#### `agent_service_start`\nStart the agent service (if not running).\n```\nParameters: (none)\nResponse: \"Service started (PID: {pid})\" or \"Service already running (PID: {pid})\"\n```\nNote: usually auto-started, this is for manual control.\n\n#### `agent_service_stop`\nStop the agent service (stops all agents).\n```\nParameters:\n  force?: boolean         — kill without graceful shutdown (default: false)\nResponse: \"Service stopped. {N} agents disposed.\"\n```\n\n#### `agent_service_status`\nGet service health info.\n```\nParameters: (none)\nResponse: PID, uptime, agent count, total cost, total messages, queue depths\n```\n\n---\n\n### 5. Trace / Debug\n\n#### `agent_trace`\nQuery the trace log.\n```\nParameters:\n  agent?: string          — filter by agent\n  type?: string           — filter by entry type (message, state_change, delivery, etc.)\n  last?: number           — last N entries (default: 20)\nResponse: formatted trace entries\n```",
          "addedAt": "2026-03-15T22:35:52.302Z"
        },
        {
          "type": "comment",
          "content": "## Agent-Side Tools (registered by service per agent)\n\n#### `send_message`\nSend a message to another agent or manager.\n```\nParameters:\n  to: string, subject: string, message: string,\n  important?: boolean, thread_id?: string\n```\n\n#### `context_handoff`\nSave state for context reset.\n```\nParameters:\n  summary: string, continueMessage: string\n```\n\n#### `list_messages`\nQuery own sent/received message history.\n```\nParameters:\n  thread_id?: string      — filter by thread\n  limit?: number          — default: 10\nResponse: recent messages with from, to, subject, body preview, timestamp\n```\n\n#### `list_threads`\nSee own active threads.\n```\nParameters: (none)\nResponse: threads the agent participates in, with message counts and last activity\n```\n\n### On Session Start / Context Reset\nService injects a message overview into the agent's first prompt:\n```\n## 📬 Message Overview\n- 3 unread messages waiting\n- Active threads:\n  - \"Feature X implementation\" with manager (12 msgs, last: 2min ago)\n  - \"Code review feedback\" with reviewer (5 msgs, last: 10min ago)\n- Last context reset: 5 min ago\n- Handoff summary: {summary from previous session}\n```\n\nThis matches the existing pattern from core/shared/messaging.ts startup injection.",
          "addedAt": "2026-03-15T22:38:01.508Z"
        },
        {
          "type": "comment",
          "content": "## Manager Monitor/Loop Tools\n\n#### `manager_monitor`\nPeriodic oversight loop — the manager runs unattended doing cycles of check/act/sleep.\n```\nParameters:\n  action: \"start\" | \"next\" | \"stop\"\n\nstart → resets turn counter, loads MONITOR.md instructions + FINDINGS.md, begins loop\nnext  → sleeps configured interval (countdown widget), then injects next cycle prompt\nstop  → stops loop, resets counter\n\nConfig: interval_seconds, max_turns (from settings.json)\n```\nState persists to file so it survives context resets.\n\n#### `manager_defer`\nStore an unsolvable issue for follow-up (agent is running unattended, can't ask human).\n```\nParameters:\n  title: string           — short description\n  details: string         — full context, what was tried, why it's blocked\n  priority?: \"low\" | \"medium\" | \"high\"\nResponse: \"Deferred issue #{N} stored. Will be presented on next human interaction.\"\n```\nStored in `.pi/agents/deferred.jsonl` — presented to human when they next interact.\n\n#### `manager_loop_end`\nSignal that unattended work for this cycle is complete. Triggers the countdown/wait.\n```\nParameters:\n  summary: string         — what was accomplished this cycle\nResponse: triggers countdown, then next cycle starts\n```\n\n### Session Start Warnings\n\nOn extension load (`session_start`):\n- **Check TMUX**: if `process.env.TMUX` is not set, inject warning:\n  ```\n  ⚠️ WARNING: Pi is NOT running in tmux. Context restart is NOT possible.\n  The manager CANNOT self-restart on context cutoff. Start pi inside tmux for full functionality.\n  ```\n- **Check service**: if agent service not running, inject info about starting it\n\n### Context Cutoff (Manager)\n- Manager extension monitors its own context via `before_agent_start`\n- At polite threshold: inject \"Wrap up and prepare for context restart\"\n- At hard threshold: inject \"STOP. Prepare context restart NOW.\"\n- Manager calls `/new` via tmux injection to restart (requires tmux)\n- Monitor state persists across restart via `monitor-state.json`",
          "addedAt": "2026-03-15T22:42:11.569Z"
        },
        {
          "type": "comment",
          "content": "## Status Bar\n\nMode always visible via `ctx.ui.setStatus()`:\n\n```\n🏗️ SETUP | 0 agents | service: running\n🟢 OPERATE | 3 agents (2 idle, 1 working) | service: running  \n🔄 LOOP 4/10 | 3 agents | ⏱️ 7:23 | 2 subscriptions | service: running\n🔧 CONFIGURE | 3 agents | service: running\n🐛 DEBUG | 3 agents | service: running\n```\n\nIn loop mode also shows: cycle count, countdown timer, active subscription count.\n\nImplementation: `pi.on(\"agent_end\")` + polling interval updates the status text. Mode icon changes with `manager_mode` tool call.",
          "addedAt": "2026-03-15T22:52:59.110Z"
        },
        {
          "type": "comment",
          "content": "## Default Subscriptions\n\nTwo subscriptions are **always active by default** on every session start:\n- `agent_message` — messages arriving for manager\n- `agent_state_change` — agent goes stuck/error/stopped\n\nThe manager can temporarily mute them in the current session:\n```\nmanager_unsubscribe type:agent_message    → muted for this session\nmanager_unsubscribe type:agent_state_change → muted for this session\n```\n\nBut on context restart / new session, they come back. These are NOT persisted as \"disabled\" — they're hardcoded defaults that reset.\n\nCustom subscriptions (file_modified, log_error, etc.) are the opposite — they persist across restarts and must be explicitly removed.",
          "addedAt": "2026-03-15T22:54:11.530Z"
        },
        {
          "type": "comment",
          "content": "## System Prompt Assembly (hardcoded, automatic)\n\n### Agent system prompt (built by service):\n```\n1. Agent's SYSTEM.md              ← user-written, from .pi/agents/<name>/SYSTEM.md\n2. Coding guidelines              ← hardcoded, auto-included if agent has coding tools\n3. Unattended agent instructions  ← HARDCODED in service, always appended\n4. Message overview               ← auto-generated on start/reset (threads, unread count)\n5. Handoff summary                ← auto-injected after context reset (from previous session)\n```\n\n### Manager system prompt (built by extension):\n```\n1. Manager's SYSTEM.md            ← from .pi/agents/manager/SYSTEM.md\n2. Mode instructions              ← HARDCODED per mode, auto-injected on mode switch\n3. Monitor state                  ← auto-injected if loop is active\n4. Message overview               ← auto-generated on start/reset\n```\n\n**None of the hardcoded parts are editable by the user.** They're framework internals. The user only writes SYSTEM.md files.",
          "addedAt": "2026-03-15T23:02:16.071Z"
        }
      ],
      "createdAt": "2026-03-15T22:35:24.675Z",
      "updatedAt": "2026-03-15T23:04:50.227Z",
      "autoValidation": {
        "type": "human",
        "strategy": "User confirms the tool inventory covers all needed operations"
      },
      "closeMessage": "Complete. 52 tools mapped across 5 modes. Reusable specs in 3 assets.",
      "closeReason": "done",
      "validations": [
        {
          "criterion": "Map all tools the manager extension registers, with complete parameter schemas and response formats.\n\nThese are the tools the human operator (or future manager agent) uses to control the agent service.\n\nCategories:\n1. **Agent management** — spawn, stop, restart, list\n2. **Messaging** — send message, view all messages, view threads\n3. **Observability** — status, tail, peek, dashboard\n4. **Service** — start service, stop service, service status\n5. **Configuration** — reload config, view config\n\nFor each tool: name, description, parameters (TypeBox schema), response format, example usage.",
          "evidence": "User confirmed review and close. \"Yes can we review the other epics/issues\". All reusable content in assets, research notes are supporting evidence only.",
          "met": true
        }
      ],
      "closedAt": "2026-03-15T23:04:50.227Z"
    },
    {
      "id": "dnvlaeq0",
      "type": "feature",
      "title": "Design settings.json schema",
      "description": "Design the complete `.pi/agents/settings.json` schema. This is the single source of configuration for the entire agent framework.\n\nMust cover:\n- Global defaults (provider, model, thinking level)\n- ACL (who can message who, agent briefs)\n- Per-agent config (model overrides, extensions, cutoff thresholds, spawn settings)\n- Monitor/loop settings (interval, max_turns)\n- Service settings (socket path, log level)\n\nDeliverable: A complete JSON schema as an asset, with an example settings.json.",
      "status": "closed",
      "epicId": "iduwan11",
      "linkedIssueIds": [],
      "questions": [],
      "research": [
        {
          "type": "reference",
          "content": "## Inputs for Schema Design\n\nSources to pull from:\n- Existing `config/acl.json` — current ACL format\n- Existing `config/agents/worker/agent.json` — current per-agent config\n- Requirements asset (1hfs3f9f) — configuration section\n- Tool inventory asset (5c5nibh6) — mode/tool mappings reference\n- Spike findings — what settings the SDK needs (model, thinking, extensions, compaction disabled)",
          "addedAt": "2026-03-15T23:07:47.074Z"
        },
        {
          "type": "comment",
          "content": "## No Path Leaking Rule\n\n**CRITICAL:** No tool response or system prompt injection should ever contain filesystem paths. All agent references use `name` only.\n\n- ✅ `agent_config_show name:worker` → shows config fields, NOT the folder path\n- ✅ `agent_list` → shows names + status, NOT paths\n- ✅ `agent_system_prompt_show name:worker` → shows content, NOT file path\n- ✅ `agent_trace` → shows events by agent name, NOT file locations\n- ✅ Extension names shown as short names (\"project-management\"), NOT resolved paths\n\nThe service resolves names → paths internally. Tools only accept and return names.\n\n## Gap Fix: agent_create / agent_delete\n\n`agent_create` must:\n1. Create folder + agent.json + SYSTEM.md\n2. Add path to settings.json `agents[]` array\n\n`agent_delete` must:\n1. Remove path from settings.json `agents[]` array\n2. Optionally remove folder (with confirmation)\n\nBoth tools accept a `name` only — the service determines where to create/find the folder (defaults to `.pi/agents/{name}/`).",
          "addedAt": "2026-03-15T23:13:56.068Z"
        },
        {
          "type": "comment",
          "content": "## agent_register / agent_unregister\n\n#### `agent_register`\nRegister an agent. Creates folder + files if needed, or registers an existing folder.\n```\nParameters:\n  name: string              — agent name (for new agents)\n  path?: string             — optional folder path (default: .pi/agents/{name}/)\n                               use absolute path for agents outside the repo\nResponse: \"Agent {name} registered\"\n```\n- If path exists with agent.json → registers it as-is\n- If path doesn't exist → creates folder + agent.json + SYSTEM.md skeleton\n\n#### `agent_unregister`\nRemove agent from settings.json. Does NOT delete the folder.\n```\nParameters:\n  name: string\nResponse: \"Agent {name} unregistered (folder preserved)\"\n```\n\nReplaces `agent_create` / `agent_delete` in the tool inventory.\n\n## Debug Mode Path Visibility\n\nIn DEBUG mode only, tools reveal internal details:\n- `agent_inspect` shows: folder path, resolved extension paths, socket path, PID\n- `agent_list` in debug adds a \"path\" column\n- `config_show` shows full settings.json including paths\n\nIn all other modes, paths are hidden — only names shown.",
          "addedAt": "2026-03-15T23:14:53.486Z"
        },
        {
          "type": "comment",
          "content": "## agent_register Scaffold Behavior\n\nWhen registering and the folder/agent.json doesn't exist, the tool:\n\n1. Creates the folder structure\n2. Scaffolds agent.json + SYSTEM.md + AGENTS.md\n3. Returns the scaffolded content + clear instructions\n\n**Response on new agent scaffold:**\n```\nAgent \"researcher\" registered and scaffolded.\n\n## Created: agent.json\n{\n  \"name\": \"researcher\",\n  \"brief\": \"TODO: Describe what this agent does in 1-2 sentences.\",\n  \"extensions\": [],\n  \"coding_tools\": true,\n  \"auto_spawn\": false\n}\n\n## Created: SYSTEM.md\n# Agent: researcher\n\nTODO: Define this agent's role, responsibilities, and behavior.\n\n## Created: AGENTS.md\n(empty — agent will write learnings here)\n\n## Next Steps\n1. Edit agent.json — set the `brief` (other agents see this)\n2. Edit SYSTEM.md — define the agent's role and instructions\n3. Configure ACL — add messaging permissions in settings\n4. Optionally set model/provider/extensions overrides in agent.json\n5. Use `agent_spawn` to start the agent when ready\n```\n\nThis way the manager (or user in setup mode) sees exactly what was created and knows what to do next.",
          "addedAt": "2026-03-15T23:16:31.966Z"
        }
      ],
      "createdAt": "2026-03-15T23:07:36.485Z",
      "updatedAt": "2026-03-15T23:20:27.012Z",
      "autoValidation": {
        "type": "human",
        "strategy": "User confirms the schema covers all configuration needs"
      },
      "closeMessage": "settings.json schema complete. All details in asset.",
      "closeReason": "done",
      "validations": [
        {
          "criterion": "Design the complete `.pi/agents/settings.json` schema. This is the single source of configuration for the entire agent framework.\n\nMust cover:\n- Global defaults (provider, model, thinking level)\n- ACL (who can message who, agent briefs)\n- Per-agent config (model overrides, extensions, cutoff thresholds, spawn settings)\n- Monitor/loop settings (interval, max_turns)\n- Service settings (socket path, log level)\n\nDeliverable: A complete JSON schema as an asset, with an example settings.json.",
          "evidence": "User confirmed \"yes do so\". Complete schema in asset n2j36dl7 covering settings.json, agent.json, folder structure, config resolution, no-path-leaking rule, agent_register scaffold, extension resolution.",
          "met": true
        }
      ],
      "closedAt": "2026-03-15T23:20:27.012Z"
    },
    {
      "id": "8a7e2rjx",
      "type": "feature",
      "title": "Design remaining API surfaces: service, protocol, state machine, trace, events",
      "description": "Complete the remaining 5 requirement specs for the epic:\n1. Service API surface (AgentManager methods)\n2. Protocol commands/events for Unix socket adapter\n3. Agent state machine transitions and health thresholds\n4. Trace entry types and query API\n5. Event subscription model with bounded delivery",
      "status": "closed",
      "epicId": "iduwan11",
      "linkedIssueIds": [],
      "questions": [],
      "research": [
        {
          "type": "reference",
          "content": "Key inputs from existing requirements and spikes:\n\n**Service API** — from requirements asset:\n- Uses SDK `createAgentSession` for all agents\n- Transport-agnostic core — protocol adapters wrap it\n- State recovery from trace.jsonl replay\n- Singleton per project\n\n**Protocol** — from spike 44uom9qq:\n- Unix socket with newline-delimited JSON\n- Request/response with `id` field + server-pushed events\n- Manager extension is the client\n\n**State Machine** — from requirements:\n- States: offline → online-idle → online-working (→ online-blocked future)\n- Health: healthy / slow / stuck via token gap timing\n- Context cutoff: polite (70%) and hard (90%) thresholds\n\n**Trace** — from requirements:\n- Append-only JSONL\n- Every message, state transition, spawn/stop, tool call\n- Queryable by agent, thread, time range, type\n- Replayable for state recovery\n\n**Events** — from requirements:\n- Bounded subscriptions (max N then auto-unsubscribe)\n- Filters: event type, agent name, thread\n- Prevents infinite loops",
          "addedAt": "2026-03-15T23:28:02.933Z"
        },
        {
          "type": "comment",
          "content": "**⚠️ VALIDATION STATUS: These 5 requirement assets are NOT yet validated by the user.** They are reference designs that should be followed as the default plan, but can be deviated from during implementation if practical concerns arise. They are not hard constraints — treat them as strong guidance that can be broken with justification.\n\nAssets pending validation:\n- Service API Surface (AgentManager) — `i5eisc9o`\n- Protocol: Unix Socket Commands & Events — `4o3g4qf3`\n- Agent State Machine & Health Detection — `ik1yp2tc`\n- Trace Entry Types & Query API — `f5z68c4v`\n- Event Subscription Model — `il2m3sl0`",
          "addedAt": "2026-03-15T23:31:36.372Z"
        }
      ],
      "createdAt": "2026-03-15T23:27:46.676Z",
      "updatedAt": "2026-03-16T00:17:33.404Z",
      "autoValidation": {
        "type": "human",
        "strategy": "User reviews the requirement assets for completeness"
      },
      "closeMessage": "All 5 API surfaces designed. 13 requirement assets total, cross-referenced and reviewed. User validated with \"perfect close it!\"",
      "closeReason": "done",
      "validations": [
        {
          "criterion": "Complete the remaining 5 requirement specs for the epic:\n1. Service API surface (AgentManager methods)\n2. Protocol commands/events for Unix socket adapter\n3. Agent state machine transitions and health thresholds\n4. Trace entry types and query API\n5. Event subscription model with bounded delivery",
          "evidence": "All 5 specs delivered. User confirmed \"perfect close it!\" — validating completeness of all requirement assets.",
          "met": true
        }
      ],
      "closedAt": "2026-03-16T00:17:33.404Z"
    },
    {
      "id": "b7lmhcms",
      "type": "spike",
      "title": "Plan implementation issues from requirement assets",
      "description": "Read all 13 requirement assets, review the two implementation epics (agent service + manager extension), and break them down into concrete, ordered implementation issues. Update the epic todos to match. Consider dependencies between service and extension — service must be buildable/testable first.",
      "status": "closed",
      "epicId": "nptk1ujw",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-16T00:19:46.865Z",
      "updatedAt": "2026-03-16T00:27:19.952Z",
      "autoValidation": {
        "type": "human",
        "strategy": "User reviews the implementation plan and issue breakdown"
      },
      "closeMessage": "Planning spike complete. 10 implementation issues created in dependency order covering all agent service components. User approved.",
      "closeReason": "done",
      "validations": [
        {
          "criterion": "Read all 13 requirement assets, review the two implementation epics (agent service + manager extension), and break them down into concrete, ordered implementation issues. Update the epic todos to match. Consider dependencies between service and extension — service must be buildable/testable first.",
          "evidence": "User approved: \"perfect its approved\" — confirming the 10-issue implementation breakdown for the agent service epic.",
          "met": true
        }
      ],
      "closedAt": "2026-03-16T00:27:19.952Z"
    },
    {
      "id": "99j9fes8",
      "type": "chore",
      "title": "Scaffold pi-agent-service package",
      "description": "Create the `pi-agent-service/` package structure:\n\n- `package.json` with dependencies (`@mariozechner/pi-ai` for SDK, `@sinclair/typebox`, `nanoid`)\n- `tsconfig.json` \n- `src/` directory with skeleton `index.ts` (CLI entry point — just arg parsing + \"not implemented\")\n- Shared types file (`src/types.ts`) with all core interfaces: `ManagedAgent`, `AgentState`, `AgentConfig`, `Message`, `ThreadSummary`, `TraceEntry`, `TraceEntryType`, `Settings`, `AclRule`, `EventFilter`, `Subscription`\n\nThis is pure scaffolding — no logic, just structure and types from the requirement assets.",
      "status": "draft",
      "epicId": "nptk1ujw",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-16T00:23:08.678Z",
      "updatedAt": "2026-03-16T00:23:08.678Z",
      "autoValidation": {
        "type": "agent",
        "strategy": "Verify package.json is valid, types.ts compiles, and directory structure matches the reference file structure asset"
      }
    },
    {
      "id": "ldj9694v",
      "type": "feature",
      "title": "Settings loader with fs.watch and default merging",
      "description": "Implement `src/settings.ts` — the `SettingsLoader` class:\n\n- `SettingsLoader.create(projectRoot, settingsPath?)` — async factory\n- Reads and parses `settings.json` with full schema validation\n- Deep merges missing fields into existing files (both settings.json and per-agent agent.json)\n- `fs.watch()` on settings.json — on change, re-reads and updates in-memory state\n- Emits change events (simple EventEmitter) so other components react to ACL/config changes\n- `getSettings()`, `getAgentConfig(name)`, `getAcl()`, `reloadSettings()`\n- `updateAcl(rules)` — writes back to settings.json\n\nSchemas defined in asset [n2j36dl7]. No dependency on other service components.",
      "status": "draft",
      "epicId": "nptk1ujw",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-16T00:23:17.326Z",
      "updatedAt": "2026-03-16T00:23:17.326Z",
      "autoValidation": {
        "type": "agent",
        "strategy": "Write unit tests that verify: default creation, field merging, ACL read/write, change event emission"
      }
    },
    {
      "id": "ppztb9ue",
      "type": "feature",
      "title": "Trace writer and query API",
      "description": "Implement `src/trace.ts` — the `TraceWriter` class:\n\n- Append-only JSONL to `service.trace_file` path\n- `append(entry)` — assigns nanoid `id`, ISO timestamp, writes line\n- `query(opts)` — sequential scan from end, filters by agent/type/threadId/after/before/limit\n- All 13 entry types from asset [f5z68c4v]\n- File auto-created on first write\n- Graceful handling of missing/corrupt file\n\nNo dependency on other service components — standalone module.",
      "status": "draft",
      "epicId": "nptk1ujw",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-16T00:23:25.130Z",
      "updatedAt": "2026-03-16T00:23:25.130Z",
      "autoValidation": {
        "type": "agent",
        "strategy": "Write tests that append entries, query with filters, verify JSONL format"
      }
    },
    {
      "id": "nqrmi7te",
      "type": "feature",
      "title": "Message router with ACL, threading, and queue",
      "description": "Implement `src/router.ts` — the `MessageRouter` class:\n\n- `sendMessage(msg)` → ACL check, thread assignment, trace, queue or deliver\n- `getMessages(opts)` / `getThreads(opts)` — query from trace\n- Thread auto-creation (new threadId if none provided)\n- Thread limit: 100 messages per thread\n- ACL enforcement: check sender→recipient permission from in-memory ACL\n- Per-agent message queues (for offline/working agents)\n- Queue drain: `drainQueue(name)` returns oldest message\n- Priority handling: normal vs important (important → steer, normal → queue)\n\nDependencies: TraceWriter (for logging), SettingsLoader (for ACL).\nDoes NOT handle delivery to sessions — that's AgentManager's job.",
      "status": "draft",
      "epicId": "nptk1ujw",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-16T00:23:34.013Z",
      "updatedAt": "2026-03-16T00:23:34.013Z",
      "autoValidation": {
        "type": "agent",
        "strategy": "Write tests: ACL allow/deny, thread creation, queue/drain, thread limit enforcement"
      }
    },
    {
      "id": "dkdythr2",
      "type": "feature",
      "title": "Agent state machine, health monitor, and cutoff",
      "description": "Implement `src/health.ts` and `src/cutoff.ts`:\n\n**State machine** (in ManagedAgent):\n- States: offline → online-idle → online-working, with transitions per [ik1yp2tc]\n- All transitions traced\n\n**Health monitor** (`HealthMonitor`):\n- Per-agent 5s interval timer, started on agent_start, stopped on agent_end\n- Token gap detection: healthy (<10s), slow (10-60s), stuck (>60s or no events 120s)\n- Emits health change events, traces transitions\n\n**Cutoff monitor** (`CutoffMonitor`):\n- Checks `session.getContextUsage().percent` on agent_end\n- Polite cutoff: enqueues warning message when threshold hit\n- Hard cutoff: sets `hardSteering` flag, steers on `tool_execution_end`\n- Reset on context_handoff\n\nDependencies: TraceWriter, SettingsLoader (for thresholds).",
      "status": "draft",
      "epicId": "nptk1ujw",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-16T00:23:43.848Z",
      "updatedAt": "2026-03-16T00:23:43.848Z",
      "autoValidation": {
        "type": "agent",
        "strategy": "Test state transitions, health detection logic with mocked timers, cutoff threshold checks"
      }
    },
    {
      "id": "c2bydlxo",
      "type": "feature",
      "title": "Prompt builder and agent tools factory",
      "description": "Implement `src/prompt-builder.ts` and `src/agent-tools.ts`:\n\n**Prompt builder:**\n- `buildSystemPrompt(agentName)` — returns string, rebuilt per turn\n- Assembly order per [gzoe99o0]: SYSTEM.md (re-read from disk) → coding guidelines → brief injection → address book (ACL-filtered) → unattended instructions\n- Address book built from in-memory ACL + agent briefs\n\n**Agent tools factory:**\n- `buildAgentTools(agentName, router, manager)` — returns `customTools` array for SDK\n- 5 tools per [0l0qkut7]: send_message, context_handoff, list_messages, list_threads, check_status\n- Each tool's execute handler wires to router/manager methods\n\nDependencies: SettingsLoader (ACL, agent configs), MessageRouter, TraceWriter.",
      "status": "draft",
      "epicId": "nptk1ujw",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-16T00:23:53.065Z",
      "updatedAt": "2026-03-16T00:23:53.065Z",
      "autoValidation": {
        "type": "agent",
        "strategy": "Test prompt assembly output includes all sections in order, test tool factory returns correct tool definitions"
      }
    },
    {
      "id": "azebrfiw",
      "type": "feature",
      "title": "Subscription manager (manager-only bounded events)",
      "description": "Implement `src/subscriptions.ts` — the `SubscriptionManager`:\n\n- `subscribe(filter, maxEvents)` → returns subscriptionId\n- `unsubscribe(id)` — cancel subscription\n- `getSubscriptions()` — list active\n- `match(traceEntry)` — check all subscriptions, return matching ones, increment counters\n- Auto-expire when `deliveredCount >= maxEvents`\n- Max 20 active subscriptions\n- No auto-renew\n\nPer [il2m3sl0]. The manager extension creates auto-subscriptions on connect (message.received, state_changed, health_changed, context_warning).\n\nNo dependency on other service components — operates on TraceEntry filter matching.",
      "status": "draft",
      "epicId": "nptk1ujw",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-16T00:24:03.127Z",
      "updatedAt": "2026-03-16T00:24:03.127Z",
      "autoValidation": {
        "type": "agent",
        "strategy": "Test subscribe/match/expire lifecycle, max subscription limit, filter matching"
      }
    },
    {
      "id": "9o5k4osh",
      "type": "feature",
      "title": "AgentManager core — session lifecycle, message delivery, event wiring",
      "description": "Implement `src/manager.ts` — the `AgentManager` class that ties everything together:\n\n- `AgentManager.create({ projectRoot })` — async factory, loads settings, inits trace, recovers state\n- `spawnAgent(name)` — creates SDK `AgentSession` with `systemPromptOverride` (function), `customTools`, session subscriptions (agent_start, agent_end, tool_execution_end, text_delta)\n- `stopAgent(name)` / `restartAgent(name)` / `compactAgent(name)`\n- `getAgentState(name)` / `listAgents()`\n- Wires session events to state machine, health monitor, cutoff, queue drain\n- Message delivery loop: on agent_end → check cutoff → drain queue → prompt if messages\n- On sendMessage while idle → immediate `session.prompt()`\n- Important messages while working → `session.steer()`\n- `context_handoff` tool triggers `session.newSession()` + rebuild + prompt\n\nThis is the integration issue — depends on all previous components. Per [i5eisc9o] and [0osnjjl4].",
      "status": "draft",
      "epicId": "nptk1ujw",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-16T00:24:15.905Z",
      "updatedAt": "2026-03-16T00:24:15.905Z",
      "autoValidation": {
        "type": "agent",
        "strategy": "Integration test with mocked SDK session: spawn agent, send message, verify state transitions and queue drain"
      }
    },
    {
      "id": "9vnl75j3",
      "type": "feature",
      "title": "Unix socket adapter (NDJSON protocol)",
      "description": "Implement `src/adapters/unix-socket.ts`:\n\n- Creates Unix domain socket server at `service.socket_path`\n- NDJSON wire format: `{ id, method, params }` → `{ id, result }` or `{ id, error }`\n- Maps all protocol commands to AgentManager methods per [4o3g4qf3]\n- Pushes subscription events as `{ event, data }` lines\n- Error codes: NOT_FOUND, ALREADY_RUNNING, NOT_RUNNING, ACL_DENIED, THREAD_LIMIT, INVALID_PARAMS, INTERNAL\n- Handles client disconnect/reconnect gracefully\n- `service.ping` returns uptime + agent count\n\nDependencies: AgentManager (all methods exposed via protocol).",
      "status": "draft",
      "epicId": "nptk1ujw",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-16T00:24:24.196Z",
      "updatedAt": "2026-03-16T00:24:24.196Z",
      "autoValidation": {
        "type": "agent",
        "strategy": "Test socket server starts, accepts connections, routes commands, returns responses in NDJSON format"
      }
    },
    {
      "id": "jo9ab8uc",
      "type": "feature",
      "title": "Service lifecycle — CLI, PID file, state recovery, graceful shutdown",
      "description": "Implement `src/index.ts` CLI entry point and `src/recovery.ts`:\n\n**CLI:**\n- `pi-agent-service --project /path` — starts the service\n- Writes PID file at `service.pid_file`\n- Singleton check: if PID file exists and process alive → exit with error\n- Signal handlers: SIGTERM/SIGINT → graceful shutdown\n\n**State recovery** (`src/recovery.ts`):\n- On startup, if trace.jsonl exists → scan and rebuild:\n  - Message queues (queued without delivered status)\n  - Thread metadata (counts, participants)\n- Do NOT re-spawn agents — they start offline\n- Trace `service_recovered` entry\n\n**Shutdown:**\n- `manager.shutdown()` → stop all agents, close trace, remove socket + PID file\n- Trace `service_stopped` entry\n\nPer [f5z68c4v] recovery section and [a3m0qqbu] runtime files.",
      "status": "draft",
      "epicId": "nptk1ujw",
      "linkedIssueIds": [],
      "questions": [],
      "research": [],
      "createdAt": "2026-03-16T00:24:33.880Z",
      "updatedAt": "2026-03-16T00:24:33.880Z",
      "autoValidation": {
        "type": "agent",
        "strategy": "Test PID file creation/cleanup, recovery from sample trace.jsonl, graceful shutdown sequence"
      }
    }
  ],
  "categories": [
    {
      "slug": "sdk-patterns",
      "description": "Proven SDK patterns for the agent framework, with spike code references"
    },
    {
      "slug": "architecture",
      "description": "Core architecture decisions and constraints for the agent framework"
    },
    {
      "slug": "requirements",
      "description": "Functional and non-functional requirements for the agent framework"
    }
  ],
  "assets": [
    {
      "id": "v9v4bbff",
      "categorySlug": "sdk-patterns",
      "title": "SDK Basic: Create Two Agents with Different Personalities",
      "context": "When creating agent sessions with the SDK — shows the fundamental pattern for spawning agents with custom system prompts, custom tools, and in-memory sessions",
      "body": "## Pattern: Multiple Agent Sessions\n\nCreate multiple `AgentSession` instances in one Node process, each with unique system prompts and tools.\n\n```typescript\nconst { session } = await createAgentSession({\n  model,\n  thinkingLevel: \"off\",\n  authStorage, modelRegistry,\n  tools: [],                          // no built-in coding tools\n  customTools: makeTools(name),       // per-agent custom tools\n  sessionManager: SessionManager.inMemory(),\n  settingsManager: SettingsManager.inMemory({ compaction: { enabled: false } }),\n  systemPromptOverride: \"You are Alice, a cheerful programmer...\",\n});\n```\n\n### Key Points\n- Each session is fully independent — no shared state\n- `tools: []` removes all built-in tools (read, bash, edit, write)\n- `customTools` provides per-agent tool sets\n- `systemPromptOverride` sets the agent's personality/role\n- `SessionManager.inMemory()` = no filesystem persistence\n- `SettingsManager.inMemory()` = no global settings\n\n### Spike Code\n`spike-sdk/main.ts` — Alice (cheerful) and Bob (grumpy) exchange 5 messages via filesystem mailbox. Sequential turn-taking orchestrated by the main process.",
      "project": false,
      "sources": [
        {
          "type": "file",
          "path": "spike-sdk/main.ts",
          "description": "Two agents exchanging messages sequentially with custom system prompts and filesystem mailbox"
        }
      ],
      "linkedEpicIds": [
        "vp84p1ys"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T21:46:22.505Z",
      "updatedAt": "2026-03-15T21:48:24.937Z"
    },
    {
      "id": "mt85tiy8",
      "categorySlug": "sdk-patterns",
      "title": "SDK Async: Fully Parallel Agent Execution",
      "context": "When running multiple agents simultaneously — shows that Promise.all works for concurrent agent prompting with no deadlocks",
      "body": "## Pattern: Async Parallel Agents\n\nRun multiple agents truly simultaneously via `Promise.all`. Each agent has its own async loop.\n\n```typescript\nconst results = await Promise.all([\n  runAgentLoop(alice, \"alice\", true),   // starts conversation\n  runAgentLoop(bob, \"bob\", false),      // polls for messages\n]);\n```\n\nEach loop:\n1. Check messages (filesystem mailbox)\n2. If message found, reply\n3. Wait, repeat\n\n### Key Points\n- No deadlocks — filesystem mailbox handles concurrent read/write\n- Agents interleave naturally based on LLM response times\n- Timing matters — polling delay affects message sync (use event-driven in production)\n- Multiple `session.prompt()` calls can run concurrently across sessions\n\n### Spike Code\n`spike-sdk/main-async.ts` — Both agents run simultaneously, 5 message exchanges, ~66s with Haiku.",
      "project": false,
      "sources": [
        {
          "type": "file",
          "path": "spike-sdk/main-async.ts",
          "description": "Two agents running fully async via Promise.all with concurrent mailbox polling"
        }
      ],
      "linkedEpicIds": [
        "vp84p1ys"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T21:46:38.797Z",
      "updatedAt": "2026-03-15T21:48:24.937Z"
    },
    {
      "id": "ju501a45",
      "categorySlug": "sdk-patterns",
      "title": "SDK Extensions: Loading Real Extensions (web-search)",
      "context": "When agents need capabilities from existing pi extensions — shows how to load extensions into SDK sessions via additionalExtensionPaths",
      "body": "## Pattern: Load Extensions into SDK Sessions\n\nUse `DefaultResourceLoader` with `additionalExtensionPaths` to load real pi extensions.\n\n```typescript\nconst loader = new DefaultResourceLoader({\n  cwd: process.cwd(),\n  agentDir: emptyDir,                  // blocks global discovery\n  additionalExtensionPaths: [\n    \"/path/to/web-search.ts\",          // explicit extension\n  ],\n  settingsManager: settings,\n});\nawait loader.reload();\n\nconst { session } = await createAgentSession({\n  resourceLoader: loader,\n  // ... other options\n});\n```\n\n### Key Points\n- Extensions register tools via `pi.registerTool()` as normal\n- The SDK session picks them up automatically\n- Combine with `customTools` for both extension tools and inline tools\n- Extension path: `~/.pi/packagemanager/packages/<name>/extensions/<name>.ts`\n\n### Tested\nBob loaded web-search extension, received a question from Alice via mailbox, used `web_search` to research, sent answer back.\n\n### Spike Code\n`spike-sdk/main-websearch.ts` — Alice asks, Bob researches via web-search extension, replies with findings.",
      "project": false,
      "sources": [
        {
          "type": "file",
          "path": "spike-sdk/main-websearch.ts",
          "description": "Agent with web-search extension answering questions from another agent"
        }
      ],
      "linkedEpicIds": [
        "vp84p1ys"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T21:46:54.137Z",
      "updatedAt": "2026-03-15T21:48:24.946Z"
    },
    {
      "id": "ts850j8b",
      "categorySlug": "sdk-patterns",
      "title": "SDK Isolation: Fully Controlled Agent Environment",
      "context": "When agents must be isolated from global/project extensions — shows how to create a blank-slate agent with only explicit tools and extensions",
      "body": "## Pattern: Full Agent Isolation\n\nBlock all global and project discovery by pointing dirs at empty locations.\n\n```typescript\nconst emptyAgentDir = mkdtempSync(join(tmpdir(), \"pi-agent-isolated-\"));\nconst emptyCwd = mkdtempSync(join(tmpdir(), \"pi-agent-cwd-\"));\n\nconst loader = new DefaultResourceLoader({\n  cwd: emptyCwd,              // no project extensions\n  agentDir: emptyAgentDir,    // no global extensions\n  settingsManager: SettingsManager.inMemory({ ... }),\n  systemPromptOverride: () => \"Custom prompt.\",\n});\n```\n\n### Result\n- 0 extensions, 0 skills, 0 prompts, 0 themes loaded\n- Only explicitly provided `customTools` and `additionalExtensionPaths` are available\n- System prompt is just the override + date/cwd (127 chars)\n- Auth still uses `AuthStorage.create()` for API keys (can override path)\n\n### Spike Code\n`spike-sdk/inspect-isolation.ts` — Agent created with only a custom `ping` tool, nothing else.",
      "project": false,
      "sources": [
        {
          "type": "file",
          "path": "spike-sdk/inspect-isolation.ts",
          "description": "Fully isolated agent with zero discovery — only custom ping tool"
        }
      ],
      "linkedEpicIds": [
        "vp84p1ys"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T21:47:06.167Z",
      "updatedAt": "2026-03-15T21:48:24.946Z"
    },
    {
      "id": "raj0pq1x",
      "categorySlug": "sdk-patterns",
      "title": "SDK Selective Access: Project Extensions Without Global Leaks",
      "context": "When agents need project database access (e.g., PM tools) but must not load global extensions — shows the cwd + empty agentDir + explicit extensions pattern",
      "body": "## Pattern: Project Access Without Global Leaks\n\nGive agents access to `.pi/` database while blocking all global extensions.\n\n```typescript\nconst loader = new DefaultResourceLoader({\n  cwd: projectRoot,           // real project — agent can access .pi/ database\n  agentDir: emptyDir,         // empty — blocks ~/.pi global extensions\n  additionalExtensionPaths: [\n    \"/path/to/pi-extension-project-management/index.ts\",\n  ],\n  settingsManager: SettingsManager.inMemory({ ... }),\n});\n```\n\n### Result\n- PM extension loads and finds `.pi/` database via cwd\n- Agent gets all 30 PM tools (epic_*, issue_*, asset_*, etc.)\n- No other global extensions leak in\n- Agent successfully called `epic_list` and `issue_list` against real project data\n\n### Config Pattern for settings.json\n```json\n{\n  \"agents\": {\n    \"worker\":     { \"extensions\": [\"project-management\", \"web-search\"] },\n    \"reviewer\":   { \"extensions\": [\"project-management\"] },\n    \"researcher\": { \"extensions\": [\"web-search\"] }\n  }\n}\n```\n\nFramework resolves extension names → paths → `additionalExtensionPaths`.\n\n### Spike Code\n`spike-sdk/inspect-pm.ts` — Isolated agent with PM extension only.\n`spike-sdk/inspect-pm-test.ts` — Agent calls PM tools and reports project status.",
      "project": false,
      "sources": [
        {
          "type": "file",
          "path": "spike-sdk/inspect-pm.ts",
          "description": "Isolated agent loading only PM extension"
        },
        {
          "type": "file",
          "path": "spike-sdk/inspect-pm-test.ts",
          "description": "Agent using PM tools to report project status from real database"
        }
      ],
      "linkedEpicIds": [
        "vp84p1ys"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T21:47:23.939Z",
      "updatedAt": "2026-03-15T21:48:24.947Z"
    },
    {
      "id": "4tiwnpp8",
      "categorySlug": "sdk-patterns",
      "title": "SDK Observability: Token Streaming, Context %, Stats & Stuck Detection",
      "context": "When monitoring agent health and resource usage — shows all available real-time observability APIs",
      "body": "## Pattern: Agent Observability\n\n### Real-time Token Streaming\n```typescript\nsession.subscribe((event) => {\n  if (event.type === \"message_update\" && event.assistantMessageEvent?.type === \"text_delta\") {\n    const delta = event.assistantMessageEvent.delta;\n    // Track time between tokens for stuck detection\n  }\n});\n```\nTokens arrive individually with timestamps. Gap > N seconds = potentially stuck.\n\n### Context Window Usage\n```typescript\nsession.getContextUsage()\n// → { tokens: 4645, contextWindow: 1000000, percent: 0.46 }\n```\n\n### Session Stats (cumulative)\n```typescript\nsession.getSessionStats()\n// → { userMessages: 2, assistantMessages: 2, toolCalls: 0,\n//     tokens: { input: 6, output: 17, cacheRead: 9226, total: 9275 },\n//     cost: 0.00523 }\n```\n\n### Per-Message Usage\nEach assistant message has `usage` field:\n```json\n{ \"input\": 3, \"output\": 5, \"cacheRead\": 0, \"totalTokens\": 4630,\n  \"cost\": { \"input\": 0.000015, \"output\": 0.000125, \"total\": 0.029 } }\n```\n\n### Streaming State\n- `session.isStreaming` — true during generation\n- `session.state` — live state object\n\n### Event Lifecycle\n`turn_start` → `text_delta`... → `turn_end` → `agent_end`\n`tool_execution_start` → `tool_execution_update`... → `tool_execution_end`\n\n### Stuck Detection Strategy\n1. Track `lastTokenTime` per agent\n2. If `isStreaming === true` but no `text_delta` for 30s → flag as stuck\n3. Count `tool_execution_start` without `agent_end` → detect infinite tool loops\n4. `session.abort()` to kill stuck agent\n\n### Spike Code\n`spike-sdk/inspect-stream.ts` — Token-by-token streaming with timestamps and gap measurement.\n`spike-sdk/inspect2.ts` — Context usage, session stats, per-message usage aggregation.",
      "project": false,
      "sources": [
        {
          "type": "file",
          "path": "spike-sdk/inspect-stream.ts",
          "description": "Token streaming with per-token timestamps and gap measurement"
        },
        {
          "type": "file",
          "path": "spike-sdk/inspect2.ts",
          "description": "Context usage %, session stats, per-message token/cost aggregation"
        }
      ],
      "linkedEpicIds": [
        "vp84p1ys"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T21:47:43.190Z",
      "updatedAt": "2026-03-15T21:48:24.947Z"
    },
    {
      "id": "4x9o4mh9",
      "categorySlug": "sdk-patterns",
      "title": "SDK Sessions: Persistent Transcripts & In-Memory Mode",
      "context": "When choosing session persistence strategy for agents — shows both in-memory and persistent JSONL options",
      "body": "## Pattern: Session Persistence\n\n### In-Memory (no disk)\n```typescript\nSessionManager.inMemory()\n// session.sessionFile → undefined\n// session.messages → available in memory, gone after dispose()\n```\n\n### Persistent (full JSONL transcript)\n```typescript\nSessionManager.create(cwd)\n// Writes to: ~/.pi/agent/sessions/<cwd-slug>/<timestamp>_<id>.jsonl\n```\n\n### What Gets Persisted (JSONL entries)\n1. Session metadata — version, id, timestamp, cwd\n2. Model changes — provider, model id\n3. Thinking level changes\n4. Every message — user and assistant, with full content\n5. Tree structure — `id`/`parentId` for branching\n6. Assistant messages include usage/cost data\n\n### For Agent Framework\n- Use `SessionManager.create(agentSessionDir)` per agent\n- Store in `.pi/agents/<name>/sessions/` → inspectable, portable\n- Same JSONL format as regular pi sessions — can open with `pi` to inspect\n- `session.messages` for live transcript access\n- `session.newSession()` for context restart (preserves session lineage)\n\n### Spike Code\n`spike-sdk/inspect-sessions.ts` — Tests both modes, dumps JSONL file contents.",
      "project": false,
      "sources": [
        {
          "type": "file",
          "path": "spike-sdk/inspect-sessions.ts",
          "description": "Tests in-memory vs persistent sessions, dumps JSONL transcript"
        }
      ],
      "linkedEpicIds": [
        "vp84p1ys"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T21:47:58.676Z",
      "updatedAt": "2026-03-15T21:48:24.948Z"
    },
    {
      "id": "aa77qln8",
      "categorySlug": "sdk-patterns",
      "title": "RPC Mode: Headless Agent Control via JSON Protocol",
      "context": "When considering process-isolated agents or manager self-restart — reference for RPC mode capabilities vs SDK",
      "body": "## RPC Mode Overview\n\nRun pi as a headless subprocess controlled via JSONL over stdin/stdout.\n\n```bash\npi --mode rpc --no-session --model \"anthropic/claude-haiku-3-5\"\n```\n\n### Commands (send to stdin)\n- `prompt` / `steer` / `follow_up` — prompting\n- `abort` — kill current operation\n- `get_state` / `get_messages` / `get_session_stats` — observability\n- `set_model` / `cycle_model` — model control\n- `compact` / `new_session` — context management\n- `bash` — execute shell commands\n\n### Events (read from stdout)\nSame event types as SDK: `agent_start/end`, `turn_start/end`, `message_update` (text_delta), `tool_execution_*`\n\n### SDK vs RPC Comparison\n| Capability | SDK | RPC |\n|-----------|-----|-----|\n| Process isolation | ❌ shared | ✅ separate |\n| Crash isolation | ❌ shared | ✅ separate |\n| Performance | ✅ direct calls | ❌ IPC overhead |\n| Simplicity | ✅ method calls | ❌ JSON serialize |\n| Extensions | ✅ explicit loading | ✅ auto-discovery |\n| Observability | ✅ direct objects | ✅ JSON events |\n\n### When to Use RPC\n- Crash isolation needed (one agent crash shouldn't kill others)\n- Memory isolation (separate V8 heaps)\n- Manager self-restart (spawn new process, kill old one)\n\n### Architecture Decision\nSDK is primary for agent runtime. RPC is fallback for manager self-restart or crash-sensitive deployments. Not yet spike-tested with code.",
      "project": false,
      "sources": [],
      "linkedEpicIds": [
        "vp84p1ys"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T21:48:15.941Z",
      "updatedAt": "2026-03-15T21:48:24.948Z"
    },
    {
      "id": "o6grg565",
      "categorySlug": "sdk-patterns",
      "title": "Service Architecture: Unix Socket Protocol for Manager ↔ Agent Service",
      "context": "When designing the communication between the manager pi extension and the agent service — the proven protocol and message flow pattern",
      "body": "## Architecture: Unix Socket Service\n\n```\nManager (pi + extension)  ←— JSONL/Unix socket —→  Agent Service (Node + SDK)\n   │                                                    │\n   ├─ agent_spawn tool ──→ spawn_agent cmd ──→ createAgentSession()\n   ├─ agent_message tool ──→ send_message cmd ──→ session.prompt()\n   ├─ agent_status tool ──→ get_status cmd ──→ getContextUsage() + getSessionStats()\n   ├─ agent_stop tool ──→ stop_agent cmd ──→ session.dispose()\n   ├─ agent_tail tool ──→ tail_agent cmd ──→ subscribe(text_delta)\n   │\n   └─ pi.sendUserMessage() ←── agent_message event ←── agent calls send_message tool\n```\n\n### Protocol: JSONL over Unix Socket\n- Socket path: `/tmp/pi-agent-service.sock`\n- One JSON object per line, `\\n` delimited\n- Bidirectional: commands in, events out\n\n### Commands (Manager → Service)\n- `spawn_agent` — name, systemPrompt, extensions[]\n- `stop_agent` — name\n- `send_message` — from, to, content (service routes)\n- `get_status` — returns all agent stats\n- `tail_agent` — enable/disable text delta streaming\n\n### Events (Service → Manager)\n- `agent_spawned` / `agent_stopped`\n- `agent_message` — message routed to manager\n- `all_status` — per-agent context %, tokens, cost, streaming state\n- `agent_tail` — text deltas for real-time output\n- `agent_error` — error notifications\n\n### Message Routing\n- Service is the central router — agents don't talk directly\n- Agents have a `send_message` tool that calls back to the service\n- Service checks if target is \"manager\" → push event, or another agent → `session.prompt()`\n- All messages persisted to `messages.log` for audit trail\n- Use `prompt()` when agent idle, `followUp()` when streaming\n\n### Extension Integration\n- Extension connects on `session_start`, reconnects on disconnect\n- `pi.sendUserMessage()` injects agent messages into manager conversation\n- Tools are simple command senders with response waiters\n\n### Spike Code\n- `spike-service/protocol.ts` — shared types and JSONL helpers\n- `spike-service/service.ts` — the agent service\n- `spike-service/extension/index.ts` — the manager extension\n- `spike-service/test-client.ts` — standalone protocol test",
      "project": false,
      "sources": [
        {
          "type": "file",
          "path": "spike-service/protocol.ts",
          "description": "Shared protocol types, JSONL helpers, socket path"
        },
        {
          "type": "file",
          "path": "spike-service/service.ts",
          "description": "Agent service: SDK session management, Unix socket server, message routing"
        },
        {
          "type": "file",
          "path": "spike-service/extension/index.ts",
          "description": "Manager extension: connects to service, registers agent tools, injects messages"
        },
        {
          "type": "file",
          "path": "spike-service/test-client.ts",
          "description": "Standalone test client proving the full protocol flow"
        }
      ],
      "linkedEpicIds": [
        "vp84p1ys"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T21:58:53.183Z",
      "updatedAt": "2026-03-15T21:58:59.670Z"
    },
    {
      "id": "2cdwcc8l",
      "categorySlug": "architecture",
      "title": "Transport-Agnostic Service API — Critical Constraint",
      "context": "When building the agent service — ALL service logic must be transport-agnostic. The protocol layer is a thin adapter.",
      "body": "## CRITICAL: Transport-Agnostic Architecture\n\nThe AgentService MUST be a pure TypeScript API class with no transport dependencies. All communication protocols are thin adapters layered on top.\n\n### Core API (no transport)\n```typescript\nclass AgentService {\n  spawnAgent(name, config): Promise<void>\n  stopAgent(name): Promise<void>\n  sendMessage(from, to, content): void\n  getStatus(): Record<string, AgentStatus>\n  getAgentStatus(name): AgentStatus\n  peekAgent(name, lines): string[]\n  getAgentHistory(name): Message[]\n  subscribe(callback: (event: ServiceEvent) => void): unsubscribe\n}\n```\n\n### Transport Adapters (thin wrappers)\n- **UnixSocketAdapter** — current implementation, JSONL framing\n- **WebSocketAdapter** — future, for remote/browser clients\n- **RestAdapter** — future, for HTTP API access\n- **DirectAdapter** — future, for in-process use (manager as SDK agent)\n\n### Why This Matters\n1. Manager extension today uses Unix socket — but later the manager itself may become an agent inside the service (DirectAdapter)\n2. Web UI / dashboard will need WebSocket or REST\n3. Remote agents or cloud deployment needs network transport\n4. Testing is easier with direct API calls\n\n### Rule\n**Never put business logic in the transport layer.** The adapter only serializes/deserializes and forwards to the core API.",
      "project": true,
      "sources": [],
      "linkedEpicIds": [],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T22:01:34.979Z",
      "updatedAt": "2026-03-15T22:01:34.979Z"
    },
    {
      "id": "1hfs3f9f",
      "categorySlug": "requirements",
      "title": "Agent Framework Requirements",
      "context": "When designing or implementing any part of the agent framework — the master checklist of all requirements",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change. Not all items are validated — treat as strong guidance, not hard constraints.\n\n## Agent Framework — Requirements\n\n### Service Architecture\n- [ ] Single agent service process (TypeScript, uses Pi SDK)\n- [ ] Manager pi session connects via Unix socket (WebSocket/REST later)\n- [ ] Service is independent of manager — survives manager restart\n- [ ] Manager reconnects and picks up queued messages on restart\n- [ ] Singleton guarantee — only one service instance per project\n- [ ] Service auto-started by manager extension if not running\n- [ ] Transport-agnostic core API — protocol adapters are thin wrappers\n- [ ] All agent sessions managed via SDK `createAgentSession`\n- [ ] State recovery on service crash — replay trace.jsonl to rebuild queues/threads\n- [ ] systemPromptOverride fully replaces pi default — agent SYSTEM.md + framework instructions\n\n### Agent Lifecycle\n- [ ] Agents spawned with custom system prompts, tools, and extensions\n- [ ] Agent states: offline → online-idle → online-working → see [ik1yp2tc]\n- [ ] All state transitions logged\n- [ ] Health monitoring: healthy / slow / stuck detection via token gap timing → see [ik1yp2tc]\n- [ ] Agents fully isolated — empty agentDir, explicit extension loading only\n- [ ] cwd always project root\n- [ ] Agent context restart (`session.newSession()`) and compaction (`session.compact()`)\n- [ ] Clean agent stop with `session.dispose()`\n\n### Context Cutoff — Two-Level\n- [ ] Service monitors `session.getContextUsage().percent` per agent\n- [ ] NEVER interrupt a tool call — always wait for tool to complete\n- [ ] **Polite cutoff** (configurable, e.g. 70%) — after current turn completes, queue handoff prompt\n- [ ] **Hard cutoff** (configurable, e.g. 90%) — steer between every tool call until agent complies\n- [ ] Service captures handoff → `session.newSession()` → prompts with continueMessage\n- [ ] Both thresholds configurable per agent in settings.json\n- [ ] `context_handoff` tool registered automatically for every agent\n- [ ] Full flow documented in [ik1yp2tc]\n\n### Messaging\n- [ ] Service is the central message router — no direct agent-to-agent\n- [ ] ALL messages traced and persisted (append-only JSONL trace log)\n- [ ] Manager can see ALL messages — including agent-to-agent\n- [ ] Messages have: id, threadId, from, to, subject, body, priority, delivery mode, status\n- [ ] Message statuses: queued → delivered → read → (replied via new message)\n- [ ] Delivery modes: `persist` (default, queued if offline) and `online-only` (dropped if offline, still traced)\n- [ ] Priority: `normal` (queue order) and `important` (front of queue, can interrupt via steer)\n- [ ] Queue per agent — delivered when agent is idle → **triggers a new turn** via `session.prompt()`\n- [ ] Manager queue — pushed via socket events, flushed on reconnect\n\n### Event Subscriptions — Manager-Only\n- [ ] Manager subscribes to service events via protocol → see [il2m3sl0]\n- [ ] Subscriptions are bounded — max N events then auto-unsubscribe\n- [ ] Agents do NOT have subscriptions — they are message-driven only\n- [ ] Prevents infinite event → reaction → event loops\n\n### Threading\n- [ ] Messages belong to threads\n- [ ] Thread auto-created on first message\n- [ ] Max 100 messages per thread (loop prevention) — rejected with instruction to start new\n- [ ] Thread metadata: id, subject, participants, message count, timestamps\n- [ ] Reply-to support — message references parent message ID\n\n### Observability\n- [ ] Per-agent status: context %, tokens, cost, message stats, health, state\n- [ ] Per-agent conversation peek (last N lines of output)\n- [ ] Per-agent message history\n- [ ] Aggregate dashboard: all agents at a glance\n- [ ] Real-time agent tailing → separate epic [p09o4o3f] (user-facing, not manager tool)\n\n### Trace / Audit\n- [ ] Append-only JSONL trace log — never modified → see [f5z68c4v]\n- [ ] Every message logged (including failed online-only)\n- [ ] Every state transition logged\n- [ ] Every agent spawn/stop logged\n- [ ] Queryable by agent, thread, time range, type\n- [ ] Replayable — service can rebuild state from trace on restart\n\n### Configuration\n- [ ] Single settings file: `.pi/agents/settings.json` → see [n2j36dl7]\n- [ ] Contains: ACL, defaults (provider, model), per-agent paths\n- [ ] Per-agent config in agent.json: extensions list, cutoff thresholds, coding_tools flag\n- [ ] Per-agent subfolder for SYSTEM.md, AGENTS.md\n- [ ] Agent folders are portable — can live anywhere on filesystem\n\n### Manager\n- [ ] Runs in tmux (for self-restart on context cutoff)\n- [ ] Has extension with mode-based tools → see [5c5nibh6]\n- [ ] Receives agent messages via socket events → injected via `pi.sendUserMessage()`\n- [ ] Can view all inter-agent messages (full visibility)\n- [ ] May later become an agent inside the service (future-proof)\n\n### Extensions & Tools\n- [ ] Agents get tools via `customTools` + `additionalExtensionPaths`\n- [ ] Per-agent extension config in agent.json\n- [ ] Agent-side tools registered by service → see [0l0qkut7]\n- [ ] Agents can use project extensions (PM, web-search, etc.) when configured\n- [ ] No global extension leakage — only explicitly configured extensions load\n\n### Future (not MVP but design for)\n- [ ] WebSocket transport adapter\n- [ ] REST transport adapter\n- [ ] Manager as agent inside the service (DirectAdapter)\n- [ ] Web UI / dashboard\n- [ ] Agent-to-agent direct communication (bypassing manager)\n- [ ] Session persistence per agent (JSONL transcripts)\n- [ ] Multiple projects / multi-tenant",
      "project": true,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T22:13:39.259Z",
      "updatedAt": "2026-03-15T23:43:48.910Z"
    },
    {
      "id": "bizv7l7c",
      "categorySlug": "sdk-patterns",
      "title": "Service Core: Transport-Agnostic AgentManager with Queue & State Machine",
      "context": "When building the agent service — the proven core API pattern with message queue, state machine, trace, and health monitoring",
      "body": "## Pattern: AgentManager Core API\n\nTransport-agnostic service that manages agent sessions, message queues, and state.\n\n### Components\n- **`AgentManager`** — spawns agents, routes messages, tracks state, emits events\n- **`MessageQueue`** — per-agent FIFO with priority, threading, delivery tracking\n- **`Trace`** — append-only JSONL audit log, replayable\n\n### Key APIs\n```typescript\nmgr.spawnAgent(name, systemPrompt, extensions?)\nmgr.stopAgent(name)\nmgr.sendMessage(from, to, subject, body, { priority, delivery, threadId })\nmgr.getAllStatus()          // per-agent context %, tokens, cost, health\nmgr.subscribe(callback)    // events: state changes, messages, spawns\nmgr.queue.listThreads()\nmgr.trace.query({ type, agent, after, limit })\n```\n\n### Queue Delivery\n- 500ms polling loop checks all agent queues\n- Idle agent → `session.prompt(message)`\n- Busy agent + important → `session.steer(message)`\n- Offline + persist → stays queued\n- Offline + online-only → failed (still traced)\n\n### Spike Code\n- `spike-queue/agent-manager.ts` — core service API\n- `spike-queue/message-queue.ts` — queue with threading\n- `spike-queue/trace.ts` — audit log\n- `spike-queue/test-queue.ts` — integration test",
      "project": false,
      "sources": [
        {
          "type": "file",
          "path": "spike-queue/agent-manager.ts",
          "description": "Core AgentManager API — spawns, routes, tracks state"
        },
        {
          "type": "file",
          "path": "spike-queue/message-queue.ts",
          "description": "Message queue with priority, threading, delivery tracking"
        },
        {
          "type": "file",
          "path": "spike-queue/trace.ts",
          "description": "Append-only JSONL trace/audit log"
        },
        {
          "type": "file",
          "path": "spike-queue/test-queue.ts",
          "description": "Integration test proving all patterns"
        }
      ],
      "linkedEpicIds": [
        "vp84p1ys"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T22:15:50.561Z",
      "updatedAt": "2026-03-15T22:15:57.606Z"
    },
    {
      "id": "pkrsaobo",
      "categorySlug": "sdk-patterns",
      "title": "Service Lifecycle: Detached Spawn, Singleton, Reconnect",
      "context": "When implementing the service startup and management from the manager extension",
      "body": "## Pattern: Detached Service with Reconnect\n\n### Spawn\n```typescript\nconst child = spawn('npx', ['tsx', 'service.ts'], {\n  detached: true, stdio: 'ignore', env: { ...process.env },\n});\nchild.unref();\n```\n\n### Singleton: socket path is the lock\n`net.createServer().listen(path)` fails with EADDRINUSE if already bound.\n\n### PID file for stale detection\n```typescript\n// Check if existing service is alive\nconst pid = parseInt(readFileSync(PID_FILE, 'utf-8'));\nconst alive = (() => { try { process.kill(pid, 0); return true; } catch { return false; } })();\n```\n\n### Reconnect flush\nService queues messages while no client connected. On reconnect, flushes all queued messages immediately.\n\n### Graceful shutdown\nSIGTERM → dispose agents → close socket → remove PID + socket files.\n\n### Spike Code\n- `spike-lifecycle/mini-service.ts` — minimal service with queue\n- `spike-lifecycle/test-lifecycle.ts` — full lifecycle test",
      "project": false,
      "sources": [
        {
          "type": "file",
          "path": "spike-lifecycle/test-lifecycle.ts",
          "description": "Full service lifecycle test: spawn, singleton, survive, reconnect, shutdown"
        }
      ],
      "linkedEpicIds": [],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T22:24:31.363Z",
      "updatedAt": "2026-03-15T22:24:53.560Z"
    },
    {
      "id": "7rtcq7ft",
      "categorySlug": "sdk-patterns",
      "title": "Context Cutoff: Handoff Tool → newSession → Continue",
      "context": "When implementing agent context cutoff handling in the service",
      "body": "## Pattern: Context Cutoff Handoff\n\n### Flow\n1. Service monitors `session.getContextUsage().percent` per agent\n2. At threshold (e.g. 80%) → prompt agent to call `context_handoff`\n3. Agent calls `context_handoff(summary, continueMessage)`\n4. Service captures the handoff data\n5. `session.newSession()` → returns `true`, resets to 0 messages/0 tokens\n6. Session object stays alive and usable\n7. Prompt with `continueMessage` → agent continues with fresh context\n\n### Handoff Tool\n```typescript\n{\n  name: \"context_handoff\",\n  parameters: Type.Object({\n    summary: Type.String(),\n    continueMessage: Type.String(),\n  }),\n  execute: async (_id, params) => {\n    // Service captures params, triggers reset\n  },\n}\n```\n\n### Key Finding\n`session.newSession()` resets context completely (0 messages, 0 tokens) but the session object remains valid for immediate re-prompting.\n\n### Spike Code\n`spike-lifecycle/test-cutoff2.ts`",
      "project": false,
      "sources": [
        {
          "type": "file",
          "path": "spike-lifecycle/test-cutoff2.ts",
          "description": "Context handoff → newSession → continue with fresh context"
        }
      ],
      "linkedEpicIds": [],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T22:24:47.521Z",
      "updatedAt": "2026-03-15T22:24:53.563Z"
    },
    {
      "id": "j56jll9o",
      "categorySlug": "architecture",
      "title": "Pi Default Coding Guidelines (for agent SYSTEM.md templates)",
      "context": "When writing or generating SYSTEM.md files for agents that do coding work — incorporate these guidelines from pi's default system prompt",
      "body": "## Pi Coding Guidelines\n\nThese are the relevant guidelines from pi's default system prompt. Include them in SYSTEM.md for any agent that does coding work (worker, prototyper, etc.). Omit for non-coding agents (researcher, reviewer doing text-only review).\n\n### For agents with coding tools (read, bash, edit, write)\n```\n- Use bash for file operations like ls, rg, find\n- Use read to examine files before editing. You must use this tool instead of cat or sed.\n- Use edit for precise changes (old text must match exactly)\n- Use write only for new files or complete rewrites\n- When summarizing your actions, output plain text directly - do NOT use cat or bash to display what you did\n- Be concise in your responses\n- Show file paths clearly when working with files\n```\n\n### For all agents\n```\n- Be concise in your responses\n```\n\n### Design Decision: Tool Listings NOT Needed\n**All providers pi supports (Anthropic, OpenAI, Google, Mistral, Amazon Bedrock, Azure) send tool schemas via the native API.** Tools are NOT discovered from the system prompt. Tested: all major models successfully called a custom tool with zero system prompt mention.\n\nDo NOT include a tool listing in SYSTEM.md — it's redundant and wastes tokens.\n\n### System Prompt Strategy\nUse `systemPromptOverride` (full replace). The agent's system prompt is assembled by the service:\n1. Agent's SYSTEM.md (role, personality, instructions)\n2. Coding guidelines (if agent has coding tools)\n3. Framework instructions (messaging conventions, handoff instructions)\n4. Date + cwd (appended automatically by pi)",
      "project": true,
      "sources": [],
      "linkedEpicIds": [],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T22:31:21.829Z",
      "updatedAt": "2026-03-15T22:34:29.765Z"
    },
    {
      "id": "5c5nibh6",
      "categorySlug": "requirements",
      "title": "Manager Tools & Modes",
      "context": "When implementing the manager extension — tool inventory grouped by modes with dynamic switching",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change.\n\n## Modes\n\nModes control which tools are active AND inject mode-specific system prompt instructions. Persisted to `.pi/agents/manager-state.json` — survives context restart. System prompt rebuilt every turn — see [gzoe99o0].\n\n#### `manager_mode`\nSwitch the active mode. Always available.\n```\nParameters:\n  mode: \"setup\" | \"operate\" | \"loop\" | \"configure\" | \"debug\"\nResponse: lists active tools + injected instructions for this mode\n```\n\nOn mode switch:\n1. Update `manager-state.json`\n2. Enable/disable tools for new mode\n3. Next turn's `before_agent_start` injects new mode instructions into system prompt\n\n---\n\n### 🏗️ SETUP\nFor creating and configuring agents. Will NOT execute tasks or send messages.\n\n**Injected instructions:**\n```\nYou are in SETUP mode. Your job is to create, configure, and align agents.\n- Do NOT send messages to agents\n- Do NOT spawn agents\n- Focus on: creating agent definitions, writing SYSTEM.md files, configuring ACLs, setting extensions\n- You CAN peek at agent output and messages for reference while configuring\n- When done, switch to \"operate\" mode\n```\n\n**Tools enabled:** all configure tools + observability (agent_peek, agent_messages, agent_status) for reference\n\n---\n\n### 🟢 OPERATE\nInteractive mode — working with the user, directing agents, reviewing output.\n\n**Injected instructions:**\n```\nYou are in OPERATE mode. You are working interactively with the user.\n- Only send messages to agents when the user asks\n- Report status and findings to the user\n- Wait for user direction before taking action\n- Agents are UNATTENDED — they can only communicate via the message system\n- Agents will work until their task criteria are met, then go idle\n- If an agent messages you, review and respond or direct as needed\n```\n\n**Tools enabled:** agent management, messaging, observability, service management, subscription tools\n\n---\n\n### 🔄 LOOP (unattended)\nManager runs autonomously in bounded oversight cycles. **Full loop specification in [8ikfn8hs].**\n\n**Key properties:**\n- Bounded by `max_turns` (default 10) — prevents runaway costs\n- Cycle counter incremented each cycle, persisted in manager-state.json\n- Countdown between cycles with wake-on-event\n- **Keep-alive**: `agent_end` hook prevents manager from going idle mid-cycle\n- LOOP_INSTRUCTIONS.md re-read each cycle via `loop_instructions_get`\n- Manager updates its own instructions via `loop_instructions_update`\n- **On context cutoff**: must call `loop_instructions_update` before `context_handoff`\n\n**Injected instructions:**\n```\nYou are in LOOP mode running unattended. Each cycle:\n1. Call loop_instructions_get for your instructions\n2. Check messages and subscribed events\n3. Review agent status and act on issues\n4. If you learn something new, call loop_instructions_update\n5. If something is unsolvable, call manager_defer\n6. Call manager_loop_next when done (starts countdown to next cycle)\nDo NOT wait for human input — solve or defer.\nBefore calling context_handoff, always call loop_instructions_update to save learnings.\nAgents are UNATTENDED — they communicate only via messages.\nCurrent cycle: {cycle}/{max_turns}\n```\n\n**Tools enabled:** operate tools + loop controls + subscription tools\n\n---\n\n### 🔧 CONFIGURE\nEdit settings, ACLs, system prompts, agent files.\n\n**Injected instructions:**\n```\nYou are in CONFIGURE mode. Edit settings and agent configurations.\n- Do NOT spawn or message agents\n- Changes take effect on next agent spawn or config_reload\n```\n\n**Tools enabled:** all config/edit tools\n\n---\n\n### 🐛 DEBUG\nTrace, diagnostics, deep inspection.\n\n**Injected instructions:**\n```\nYou are in DEBUG mode. Inspect, diagnose, and troubleshoot.\n- Query trace logs, inspect agent state, review message delivery\n- Do NOT modify settings or send messages unless explicitly asked\n```\n\n**Tools enabled:** all debug/inspect tools\n\n---\n\n## Always-On Tools (every mode)\n\n| Tool | Purpose |\n|------|---------|\n| `manager_mode` | Switch mode (persisted to file) |\n| `agent_list` | List agents with state |\n| `agent_status` | Quick status check |\n\n---\n\n## Default Subscriptions\n\nDefined in [il2m3sl0]. Two are always-on defaults (messages to manager + agent state changes). Custom subscriptions persist across restarts.\n\n---\n\n## Status Bar\n\nAlways visible via `ctx.ui.setStatus()`:\n```\n🏗️ SETUP | 0 agents | service: running\n🟢 OPERATE | 3 agents (2 idle, 1 working) | service: running\n🔄 LOOP 4/10 | 3 agents | ⏱️ 7:23 | 2 subs | service: running\n🔧 CONFIGURE | 3 agents | service: running\n🐛 DEBUG | 3 agents | service: running\n```\n\n---\n\n## Tool Groups\n\n### Agent Management\n| Tool | Modes | Purpose |\n|------|-------|---------|\n| `agent_spawn` | operate, loop | Spawn agent from config |\n| `agent_stop` | operate, loop | Stop agent |\n| `agent_restart` | operate, loop | Context reset (handoff → newSession → continue) |\n| `agent_list` | all | List agents + state |\n\n### Messaging\n| Tool | Modes | Purpose |\n|------|-------|---------|\n| `agent_message` | operate, loop | Send message to agent |\n| `agent_messages` | operate, loop, setup, debug | View all messages (inc. agent-to-agent) |\n| `agent_threads` | operate, loop, setup, debug | List threads |\n\n### Observability\n| Tool | Modes | Purpose |\n|------|-------|---------|\n| `agent_status` | all | Context %, tokens, cost, health |\n| `agent_peek` | operate, loop, setup, debug | Last N lines of output |\n\n> Real-time tailing is a user-facing feature → epic [p09o4o3f]\n\n### Loop Controls — see [8ikfn8hs]\n| Tool | Modes | Purpose |\n|------|-------|---------|\n| `manager_monitor` | loop | Start/stop loop |\n| `manager_loop_next` | loop | Signal cycle done, start countdown |\n| `manager_defer` | loop | Urgent note to user — unsolvable issue |\n| `loop_instructions_get` | loop | Read current loop instructions |\n| `loop_instructions_update` | loop | Update instructions with learnings |\n\n### Subscriptions — see [il2m3sl0]\n| Tool | Modes | Purpose |\n|------|-------|---------|\n| `manager_subscribe` | loop, operate | Add event subscription |\n| `manager_unsubscribe` | loop, operate | Remove/mute subscription |\n| `manager_subscriptions` | loop, operate, debug | List active subscriptions |\n\nSubscription types (extensible):\n```\nParameters:\n  type: \"file_modified\" | \"json_value_change\" | \"log_error\" | \"agent_message\" | \"agent_state_change\"\n  config: {\n    path?: string, json_path?: string, agent?: string, pattern?: string\n  }\n  label?: string\n```\n\nWhen an event fires:\n- If in loop countdown → cancels countdown, injects event into conversation\n- If idle → injects as `pi.sendUserMessage()`\n\n### Configuration\n| Tool | Modes | Purpose |\n|------|-------|---------|\n| `config_show` | configure, setup | Show settings.json |\n| `config_set` | configure, setup | Update settings value |\n| `config_reload` | configure | Reload into running service |\n| `acl_show` | configure, setup | Show ACL |\n| `acl_set` | configure, setup | Update ACL |\n| `agent_config_show` | configure, setup | Show agent config |\n| `agent_config_set` | configure, setup | Update agent config |\n| `agent_system_prompt_show` | configure, setup, debug | Read SYSTEM.md |\n| `agent_system_prompt_edit` | configure, setup | Write SYSTEM.md |\n| `agent_file_show` | configure, setup, debug | Read agent folder file |\n| `agent_file_edit` | configure, setup | Write agent folder file |\n| `agent_file_list` | configure, setup, debug | List agent folder files |\n| `agent_create` | configure, setup | Create new agent definition |\n| `agent_delete` | configure, setup | Remove agent definition |\n| `agent_extensions_list` | configure, setup | List available extensions |\n| `agent_extensions_set` | configure, setup | Set agent extensions |\n| `monitor_config_show` | configure | Show monitor settings |\n| `monitor_config_set` | configure | Update monitor settings |\n\n### Debug\n| Tool | Modes | Purpose |\n|------|-------|---------|\n| `agent_trace` | debug | Query trace log |\n| `agent_history` | debug | Full conversation history |\n| `agent_inspect` | debug | Deep agent state |\n| `deferred_list` | debug, operate | List deferred items |\n| `service_log` | debug | Read service log |\n| `message_inspect` | debug | Inspect message by ID |\n| `thread_inspect` | debug | Inspect thread details |\n\n### Service\n| Tool | Modes | Purpose |\n|------|-------|---------|\n| `agent_service_start` | operate, loop | Start service |\n| `agent_service_stop` | operate | Stop service |\n| `agent_service_status` | operate, loop, debug | Service health |\n\n---\n\n## No Compaction\n\nAuto-compaction MUST be disabled for all agent sessions (`compaction: { enabled: false }`). Agents use context_handoff + newSession instead. See cutoff flow in [ik1yp2tc].\n\n---\n\n## Startup Behavior\n\n### On extension load (session_start)\n1. **tmux check**: if `TMUX` not set → inject warning\n2. **Service check**: try connect → auto-start if down\n3. **Reconnect**: flush queued manager messages\n4. **Restore mode**: load from manager-state.json, re-inject mode instructions\n5. **Restore subscriptions**: re-activate persisted custom subscriptions + always-on defaults\n6. **Restore loop**: if mode=loop, resume at persisted cycle count — see [8ikfn8hs]\n\n### On context reset\n1. Reconnect to service (it survived the restart)\n2. Inject message overview (unread, threads, handoff summary)\n3. Restore monitor state if loop was running\n4. Restore mode + subscriptions + status bar",
      "project": true,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T22:46:21.299Z",
      "updatedAt": "2026-03-16T00:09:33.437Z"
    },
    {
      "id": "gzoe99o0",
      "categorySlug": "requirements",
      "title": "System Prompt Assembly",
      "context": "When building agent or manager system prompts — assembly order, hardcoded vs user-written sections",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change.\n\n## System Prompt Assembly\n\nAll system prompts are assembled automatically. Users only write SYSTEM.md files. Prompts are **rebuilt on every turn** (before each `agent_start`) so changes to SYSTEM.md, ACL, agent briefs, etc. take effect live.\n\n---\n\n## Agent System Prompt (built by service)\n\nRebuilt via `systemPromptOverride` (a function, not static) before every turn.\n\n```\n1. Agent's SYSTEM.md              ← user-written, from {agent_path}/SYSTEM.md\n                                    Re-read from disk on every rebuild.\n\n2. Coding guidelines              ← from pi's default system prompt essentials.\n                                    Included if coding_tools=true.\n                                    Captured once at service startup from pi's defaults\n                                    and baked into the agent prompt builder.\n\n3. Brief injection                ← from agent.json:\n                                    \"## How Others See You\n                                     Other agents know this about you: \"{brief}\"\"\n\n4. Address book                   ← built from ACL + agent.json briefs (live):\n                                    \"## Available Agents\n                                     You can message:\n                                     - **reviewer**: Reviews code changes for quality and correctness.\n                                     - **manager**: Orchestrates work across agents.\n                                     You cannot message: researcher (no ACL access)\"\n\n5. Unattended agent instructions  ← HARDCODED, always appended:\n                                    \"You are an UNATTENDED agent...\"\n                                    (full text — see below)\n```\n\n### Address Book Details\n\nBuilt from in-memory ACL + agent configs. The service watches `settings.json` for changes (filesystem watcher) and updates ACL in memory without restart. Each agent sees only agents they have ACL access to, with briefs:\n\n```\n## Available Agents\nYou can message the following agents:\n\n- **reviewer** — Reviews code changes for quality and correctness.\n- **manager** — Orchestrates work across agents.\n\nTo send a message, use the `send_message` tool with the agent's name.\n```\n\nAgents without ACL access are NOT listed. If ACL is updated live, the next turn's system prompt reflects the change.\n\n### Unattended Instructions (hardcoded)\n\n```\nYou are an UNATTENDED agent. You cannot interact with humans directly.\n- Your ONLY communication channel is the message system (send_message tool)\n- Work until your task criteria are fully met\n- If blocked or unsolvable, send a message to the manager explaining the situation\n- Only go idle when ALL your assigned work is complete and no responses are pending\n- NEVER wait for human input — you will not receive any\n```\n\n### What is NOT in the system prompt\n\n- **Message overview** (unread count, threads, handoff summary) — injected as the **first user message** on session start / context reset, not in system prompt. See [0l0qkut7].\n- **Tool listings** — sent via API by the provider, not in prompt text.\n\n---\n\n## Manager System Prompt (built by extension)\n\nRebuilt via `before_agent_start` hook → `addSystemPrompt()` before every turn.\n\n```\n1. Manager's SYSTEM.md            ← user-written, from {manager_path}/SYSTEM.md\n                                    Re-read from disk on every rebuild.\n\n2. Mode instructions              ← HARDCODED per mode — see [5c5nibh6]\n                                    Injected based on current mode from manager-state.json.\n                                    Updated immediately when manager_mode is called.\n\n3. Address book                   ← same as agents: ACL-filtered agent list with briefs\n\n4. Monitor state                  ← if loop mode active:\n                                    cycle count, findings summary, instructions reference\n```\n\n### Mode Switch Behavior\n\nWhen `manager_mode` is called:\n1. Update `manager-state.json` with new mode\n2. Register/deregister tools for the new mode (see [5c5nibh6])\n3. Next turn's `before_agent_start` injects the new mode's instructions into system prompt\n\nTool registration per mode — see [5c5nibh6] for the full mapping of which tools are enabled in which mode.\n\n---\n\n## Coding Guidelines — Bootstrap\n\nThe agent service needs pi's default coding guidelines (file editing conventions, bash usage, etc.) for agents with `coding_tools=true`. These are NOT user-editable.\n\n**Approach:** On first `agent_register` or service startup, capture the essential coding portions of pi's default system prompt. Store as a static template in the service. This avoids runtime dependency on pi's internals.\n\nAlternatively: when scaffolding a new agent via `agent_register`, the generated SYSTEM.md can include pi's default coding guidelines as a starting point that the user can then customize.\n\n---\n\n## Agent Tool Availability\n\n### Agent-side (service)\n5 tools registered via `customTools` at `createAgentSession()` time — see [0l0qkut7]. Always available, never change during a session. No dynamic registration needed.\n\n### Manager-side (extension)\n~40 tools registered via `pi.registerTool()` on `session_start`. Mode switching dynamically enables/disables tools. See [5c5nibh6] for which tools are in which mode and [0osnjjl4] for the registration lifecycle.\n\n---\n\n## Settings File Watcher\n\nThe agent service watches `settings.json` with a filesystem watcher (`fs.watch`). On change:\n1. Re-read and parse settings.json\n2. Update in-memory ACL (affects next message send + next system prompt rebuild)\n3. Update in-memory agent configs (affects next spawn)\n4. Do NOT restart running agents — changes apply on next turn / next spawn\n5. Trace a config reload event\n\nThis means ACL changes, new agent additions, and default changes are live without service restart.",
      "project": true,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T23:03:53.892Z",
      "updatedAt": "2026-03-15T23:57:57.851Z"
    },
    {
      "id": "0l0qkut7",
      "categorySlug": "requirements",
      "title": "Agent-Side Tools",
      "context": "When implementing the agent service — the tools registered per agent session, and session start behavior",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change.\n\n## Agent-Side Tools\n\nRegistered by the service for every agent session via `customTools`. Not user-configurable.\n\nAgents are simple message-driven workers. They receive messages, do work, send replies. They have NO event subscriptions — if they need to know about something, the manager tells them via a message. See [il2m3sl0] for why.\n\n### `send_message`\nSend a message to another agent or the manager.\n```\nParameters:\n  to: string              — recipient (agent name or \"manager\")\n  subject: string         — short topic (5-10 words)\n  message: string         — body content (markdown)\n  important?: boolean     — priority flag, interrupts recipient (default: false)\n  thread_id?: string      — continue existing thread (omit for new)\nResponse: \"Sent to {to} | Thread: {id} (#{count})\"\n```\n\n### `context_handoff`\nSave state for context reset. Called when context is running low. See cutoff flow in [ik1yp2tc].\n```\nParameters:\n  summary: string         — what you were doing, key findings, current state\n  continueMessage: string — exact prompt for your next session to continue\nResponse: \"Handoff recorded. Session will reset.\"\n```\n\n### `list_messages`\nQuery own sent/received message history.\n```\nParameters:\n  thread_id?: string      — filter by thread\n  limit?: number          — default: 10\nResponse: recent messages with from, to, subject, body preview, timestamp\n```\n\n### `list_threads`\nSee own active threads.\n```\nParameters: (none)\nResponse: threads the agent participates in, with message counts and last activity\n```\n\n### `check_status`\nCheck own resource usage.\n```\nParameters: (none)\nResponse: context %, tokens used, cost, session uptime\n```\n\n### On Session Start / Context Reset\n\nService injects a message overview into the agent's first prompt (also part of system prompt assembly — see [gzoe99o0]):\n```\n## 📬 Message Overview\n- 3 unread messages waiting\n- Active threads:\n  - \"Feature X implementation\" with manager (12 msgs, last: 2min ago)\n  - \"Code review feedback\" with reviewer (5 msgs, last: 10min ago)\n- Last context reset: 5 min ago\n- Handoff summary: {summary from previous session}\n```",
      "project": false,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T23:04:09.939Z",
      "updatedAt": "2026-03-15T23:45:19.696Z"
    },
    {
      "id": "n2j36dl7",
      "categorySlug": "requirements",
      "title": "Settings & Config Schema",
      "context": "When implementing settings loading, validation, or config — the schema for settings.json, agent.json, folder structure, and registration",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change.\n\n## Auto-Scaffolding & Default Merging\n\nAll config files are auto-created on first extension load. **On every load**, existing files are checked for missing fields — defaults are merged in and the file is rewritten so it always reflects the full schema. The user never sees a partial config.\n\n```\nOn extension load:\n1. .pi/agents/ missing → create directory\n2. settings.json missing → create with full defaults\n   settings.json exists → deep merge missing fields from defaults, rewrite file\n3. LOOP_INSTRUCTIONS.md missing → create with default template\n4. DEFERRED.md missing → create empty\n\nOn agent_register / service reads agent.json:\n5. agent.json missing → create with full defaults\n   agent.json exists → merge missing fields from defaults, rewrite file\n6. SYSTEM.md missing → create with template\n7. AGENTS.md missing → create empty\n```\n\n**The files on disk always show every field with its current value.** If a new version of the framework adds a field, it appears in the user's files on next load with the default value.\n\n---\n\n## settings.json\n\nLocation: `.pi/agents/settings.json`\n\n### Full Default\n\n```json\n{\n  \"defaults\": {\n    \"provider\": \"anthropic\",\n    \"model\": \"claude-sonnet-4-20250514\",\n    \"thinking\": \"off\",\n    \"cutoff_polite_pct\": 70,\n    \"cutoff_hard_pct\": 90\n  },\n\n  \"service\": {\n    \"socket_path\": \"/tmp/pi-agent-service.sock\",\n    \"pid_file\": \"/tmp/pi-agent-service.pid\",\n    \"trace_file\": \".pi/agents/trace.jsonl\",\n    \"log_level\": \"info\"\n  },\n\n  \"monitor\": {\n    \"interval_seconds\": 300,\n    \"max_turns\": 10,\n    \"instructions_file\": \".pi/agents/LOOP_INSTRUCTIONS.md\"\n  },\n\n  \"acl\": [],\n\n  \"agents\": []\n}\n```\n\nACL and agents start empty — populated when user registers agents.\n\n## Field Reference\n\n### `defaults`\nInherited by all agents unless overridden in their agent.json.\n\n| Field | Type | Default | Description |\n|-------|------|---------|-------------|\n| `provider` | string | `\"anthropic\"` | LLM provider |\n| `model` | string | `\"claude-sonnet-4-20250514\"` | Model ID |\n| `thinking` | string | `\"off\"` | Thinking level |\n| `cutoff_polite_pct` | number | `70` | Context % for polite handoff — see [ik1yp2tc] |\n| `cutoff_hard_pct` | number | `90` | Context % for forced handoff — see [ik1yp2tc] |\n\n### `service`\n\n| Field | Type | Default | Description |\n|-------|------|---------|-------------|\n| `socket_path` | string | `\"/tmp/pi-agent-service.sock\"` | Unix socket path |\n| `pid_file` | string | `\"/tmp/pi-agent-service.pid\"` | PID file path |\n| `trace_file` | string | `\".pi/agents/trace.jsonl\"` | Trace log — see [f5z68c4v] |\n| `log_level` | string | `\"info\"` | Service log verbosity |\n\n### `monitor` — see [8ikfn8hs]\n\n| Field | Type | Default | Description |\n|-------|------|---------|-------------|\n| `interval_seconds` | number | `300` | Sleep between loop cycles |\n| `max_turns` | number | `10` | Max cycles before loop stops |\n| `instructions_file` | string | `\".pi/agents/LOOP_INSTRUCTIONS.md\"` | Loop instructions file |\n\n### `acl`\n\n| Field | Type | Description |\n|-------|------|-------------|\n| `from` | string | Agent name (sender) |\n| `to` | string[] | Agents this sender can message |\n\nReceive permissions derived: if A can send to B, B can receive from A.\n\n### `agents`\nArray of paths to agent folders. Relative paths resolved from project root. Absolute paths supported.\n\n---\n\n## agent.json (per-agent)\n\nLocation: `{agent_path}/agent.json`\n\n### Full Default (auto-created by `agent_register`)\n\n```json\n{\n  \"name\": \"{name}\",\n  \"brief\": \"TODO: Describe what this agent does in 1-2 sentences.\",\n  \"provider\": \"anthropic\",\n  \"model\": \"claude-sonnet-4-20250514\",\n  \"thinking\": \"off\",\n  \"cutoff_polite_pct\": 70,\n  \"cutoff_hard_pct\": 90,\n  \"extensions\": [],\n  \"coding_tools\": true,\n  \"auto_spawn\": false\n}\n```\n\nAll fields present on disk — no implicit inheritance. When the service reads agent.json, it deep merges `settings.defaults` under the agent values so user-set values always win, but the file itself shows every field.\n\n| Field | Type | Default | Description |\n|-------|------|---------|-------------|\n| `name` | string | required | Unique agent identifier. Must match ACL references. |\n| `brief` | string | required | What other agents see. Injected into system prompt — see [gzoe99o0]. |\n| `provider` | string | from defaults | LLM provider |\n| `model` | string | from defaults | Model |\n| `thinking` | string | from defaults | Thinking level |\n| `cutoff_polite_pct` | number | from defaults | Polite cutoff % |\n| `cutoff_hard_pct` | number | from defaults | Hard cutoff % |\n| `extensions` | string[] | `[]` | Extension names to load |\n| `coding_tools` | boolean | `true` | Include read/bash/edit/write tools |\n| `auto_spawn` | boolean | `false` | Auto-spawn when service starts |\n\n### Config Resolution\n\n```\n1. Service reads settings.json → gets defaults + agent paths\n2. For each path → reads {path}/agent.json\n3. Merge missing fields from settings.defaults → rewrite agent.json\n4. Runtime config = agent.json as-is (file is complete)\n5. ACL matched by agent.json.name\n```\n\n### Extension Name Resolution\n\nExtension names in `agent.json.extensions`:\n- Checks `~/.pi/packagemanager/packages/{name}/`\n- Falls back to `~/.pi/packagemanager/packages/pi-extension-{name}/`\n- Falls back to absolute paths if name contains `/`\n\n---\n\n## Auto-Created Files\n\n### SYSTEM.md (per-agent, created by `agent_register`)\n\n```markdown\n# Agent: {name}\n\nTODO: Define this agent's role, responsibilities, and behavior.\n```\n\n### AGENTS.md (per-agent, created by `agent_register`)\n\nCreated empty — agent writes learnings here.\n\n### LOOP_INSTRUCTIONS.md (created on first extension load)\n\n```markdown\n# Loop Instructions\n\n## Check Order\n1. Review all agent statuses — look for stuck or slow agents\n2. Check for unread messages from agents\n3. Check if any agents are idle that should be working\n\n## Policies\n- If an agent is stuck for >5 min, restart it\n- If a worker finishes its task, assign next available work\n- If costs are high, consider stopping idle agents\n\n## Do NOT\n- Start new work without human approval — defer these\n```\n\n### DEFERRED.md (created on first extension load)\n\nCreated empty — manager appends urgent notes via `manager_defer`.\n\n### manager-state.json (created on first mode set)\n\n```json\n{\n  \"mode\": \"operate\"\n}\n```\n\n---\n\n## Folder Structure\n\n### Per-agent folder\n```\n{agent_path}/\n├── agent.json                 ← all fields present, defaults merged in\n├── SYSTEM.md                  ← auto-created with template — see [gzoe99o0]\n└── AGENTS.md                  ← auto-created empty\n```\n\n### .pi/agents/\n```\n.pi/agents/\n├── settings.json              ← all fields present, defaults merged in\n├── manager-state.json         ← auto-created on first mode set\n├── trace.jsonl                ← auto-created by service on first write\n├── LOOP_INSTRUCTIONS.md       ← auto-created with template — see [8ikfn8hs]\n└── DEFERRED.md                ← auto-created empty — see [8ikfn8hs]\n```\n\n---\n\n## No Path Leaking Rule\n\n**CRITICAL:** No tool response or system prompt should expose filesystem paths. All agent references use `name` only.\n\n**Exception:** In DEBUG mode, `agent_inspect` reveals full paths.\n\n---\n\n## agent_register / agent_unregister\n\nManager tools (configure/setup modes — see [5c5nibh6]) and protocol commands (see [4o3g4qf3]).\n\n### `agent_register`\n```\nParameters:\n  name: string              — agent name\n  path?: string             — folder path (default: .pi/agents/{name}/)\n```\n\n| Scenario | What happens |\n|----------|-------------|\n| No path given | Creates `.pi/agents/{name}/` with full defaults |\n| Path to empty/new folder | Creates folder + full defaults |\n| Path to folder with agent.json | Merges missing fields, registers |\n| Path to folder without agent.json | Creates agent.json + SYSTEM.md, registers |\n\nAlso adds ACL entry (manager↔agent) and appends path to `settings.agents[]`.\n\n### `agent_unregister`\nRemove from settings.json. Does NOT delete the folder.\n```\nParameters:\n  name: string\n```",
      "project": false,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T23:08:31.561Z",
      "updatedAt": "2026-03-16T00:13:58.893Z"
    },
    {
      "id": "0p5tzfdo",
      "categorySlug": "sdk-patterns",
      "title": "Spike Research Index — What's Where",
      "context": "When looking for detailed type definitions, data structures, or design decisions from the spike phase — use this index to find the right issue research notes",
      "body": "## Spike Research Index\n\nQuick lookup for detailed research in closed spike issues. Use `issue_show` to read full notes.\n\n---\n\n### `otv4gdyn` — SDK createAgentSession capabilities\n- **19 confirmed SDK capabilities** with code samples\n- `systemPromptOverride` behavior (fully replaces pi default)\n- Tool schemas sent via API, NOT in system prompt\n- `before_agent_start` can append to prompt\n- `session.abort()` for stuck agents\n- `SettingsManager.inMemory()` for per-agent settings\n- Multi-session concurrency proof (Alice & Bob test)\n- Extension loading into SDK sessions (`additionalExtensionPaths`)\n- Full isolation pattern (empty agentDir + empty cwd)\n- Selective extension loading with project access\n\n### `r7ivt3j9` — RPC mode (closed wont-fix)\n- Superseded by SDK — not needed\n\n### `zebwque8` — Manager self-restart (closed wont-fix)\n- Decision: tmux for manager restart, extension warns if not in tmux\n\n### `44uom9qq` — Manager ↔ Service protocol\n- Unix socket decision rationale\n- JSONL protocol format\n- Full command/event list with flow diagram\n- `prompt()` vs `followUp()` delivery decision\n- Extension → service connection pattern\n\n### `88lkl8eb` — Message Queue, State Machine & Trace ⭐\nMost detailed design notes — contains full TypeScript types:\n- **`Message` interface** — id, threadId, from, to, subject, body, priority, delivery, replyTo, status, timestamps\n- **Delivery strategy matrix** — priority × agent state → action (prompt/steer/queue/fail)\n- **Agent state machine** — offline/idle/working/blocked + transition diagram\n- **`StateTransition` type** — agent, from, to, timestamp, reason\n- **`AgentSnapshot` type** — name, state, contextPercent, tokenUsage, cost, messageStats, health\n- **`TraceEntry` union type** — 13 event types (message, state_change, spawn, stop, tool_call, delivery, thread_created, thread_exhausted, service_start/stop, client_connect/disconnect, error)\n- **Thread rules** — auto-create, max 100 messages, exhaustion handling\n- **Service state persistence** — replay from trace.jsonl on restart\n- **Queue behavior** — FIFO with priority jump, 500ms polling, online-only vs persist modes\n\n### `zwzjxn50` — Service lifecycle & context cutoff\n- Detached spawn pattern (`child.unref()`)\n- Singleton via socket lock + PID file stale detection\n- Reconnect flush (queued messages delivered on reconnect)\n- Graceful shutdown (SIGTERM → dispose → cleanup)\n- **`session.newSession()` confirmed**: resets to 0 tokens, session object stays alive\n- Context cutoff full cycle: monitor → steer → handoff tool → newSession → continue\n- System prompt finding: override fully replaces, tools via API not prompt\n",
      "project": false,
      "sources": [],
      "linkedEpicIds": [],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T23:26:18.420Z",
      "updatedAt": "2026-03-15T23:26:18.420Z"
    },
    {
      "id": "i5eisc9o",
      "categorySlug": "requirements",
      "title": "Agent Service API",
      "context": "When implementing the agent service core — the AgentManager class, internal architecture, and key behaviors",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change.\n\n## AgentManager — Core Service API\n\nThe AgentManager is the central class in the agent service process. It owns all agent sessions, the message router, trace log, and exposes a transport-agnostic API that protocol adapters call (see [4o3g4qf3]).\n\n### Construction\n\n```typescript\nconst manager = await AgentManager.create({\n  projectRoot: string,\n  settingsPath?: string,         // default: .pi/agents/settings.json\n});\n```\n\nAsync — loads settings (see [n2j36dl7]), initializes trace (see [f5z68c4v]), recovers state from trace.jsonl if it exists.\n\n### Agent Lifecycle\n\n```typescript\nmanager.spawnAgent(name: string): Promise<void>\n// Creates SDK session with systemPromptOverride, customTools, extensions\n// See [gzoe99o0] for prompt assembly, [0l0qkut7] for agent tools\n\nmanager.stopAgent(name: string): Promise<void>\n// session.dispose(), queued messages preserved\n\nmanager.restartAgent(name: string, prompt?: string): Promise<void>\n// session.newSession() → optionally prompts\n\nmanager.compactAgent(name: string): Promise<void>\n// session.compact()\n```\n\n### Agent State\n\n```typescript\nmanager.getAgentState(name: string): AgentState\n// Returns: { name, status, health, contextPercent, tokensUsed, cost,\n//            messageStats: { sent, received, unread }, uptime, lastActivity }\n\nmanager.listAgents(): AgentSummary[]\nmanager.getAgentConfig(name: string): AgentConfig  // paths redacted\n```\n\nState machine and health detection defined in [ik1yp2tc].\n\n### Messaging\n\n```typescript\nmanager.sendMessage(msg: {\n  from: string, to: string, subject: string, body: string,\n  threadId?: string, replyTo?: string,\n  priority?: \"normal\" | \"important\",\n  delivery?: \"persist\" | \"online-only\",\n}): Promise<MessageResult>\n// Returns: { messageId, threadId, status: \"queued\" | \"delivered\" | \"dropped\" }\n\nmanager.getMessages(opts: { agent?, threadId?, limit?, before? }): Message[]\nmanager.getThreads(opts: { agent? }): ThreadSummary[]\n```\n\nACL check on every send. Thread auto-created if no threadId. Thread limit: 100 messages.\n\n### Message Delivery\n\nDelivery behavior by agent state — **messages trigger agent turns**:\n\n| Agent State | Delivery | Effect |\n|-------------|----------|--------|\n| `online-idle` | `session.prompt()` | **Starts a new turn** |\n| `online-working` + normal | Queued → delivered after `agent_end` | Triggers next turn |\n| `online-working` + important | `session.steer()` | Interrupts after current tool |\n| `offline` + persist | Queued | Delivered on next spawn |\n| `offline` + online-only | Dropped | Traced only |\n\nQueue drain loop and full delivery semantics documented in [ik1yp2tc].\n\nManager messages pushed as socket events (manager is not an SDK session).\n\n### Context Cutoff\n\nPolite and hard cutoff flows documented in [ik1yp2tc]. Service monitors `session.getContextUsage().percent` per agent.\n\n### Subscriptions (Manager-Only)\n\n```typescript\nmanager.subscribe(opts: {\n  filter: EventFilter,\n  maxEvents: number,\n}): SubscriptionId\n\nmanager.unsubscribe(id: SubscriptionId): void\nmanager.getSubscriptions(): Subscription[]\n```\n\nFull subscription model in [il2m3sl0].\n\n### Trace\n\n```typescript\nmanager.queryTrace(opts: {\n  agent?, type?, threadId?, after?, before?, limit?\n}): TraceEntry[]\n```\n\nEntry types and query details in [f5z68c4v].\n\n### Configuration\n\n```typescript\nmanager.reloadSettings(): Promise<void>\nmanager.getSettings(): Settings              // paths redacted\nmanager.updateAcl(acl: AclRule[]): Promise<void>\n```\n\n### Shutdown\n\n```typescript\nmanager.shutdown(): Promise<void>\n// Gracefully stops all agents, closes trace, removes socket/pid files\n```\n\n### Internal Architecture\n\n```\nAgentManager\n├── settings: SettingsLoader          — reads/watches settings.json\n├── agents: Map<string, ManagedAgent> — per-agent state + session\n├── router: MessageRouter             — ACL check, queue, deliver\n├── trace: TraceWriter                — append-only JSONL\n├── subscriptions: SubscriptionManager — bounded event delivery (manager-only)\n├── cutoff: CutoffMonitor             — per-agent context tracking\n└── recovery: StateRecovery           — rebuild from trace on startup\n```\n\nEach `ManagedAgent` holds:\n```typescript\ninterface ManagedAgent {\n  name: string;\n  config: AgentConfig;\n  session: AgentSession | null;    // null when offline\n  status: \"offline\" | \"online-idle\" | \"online-working\";\n  health: \"healthy\" | \"slow\" | \"stuck\";\n  queue: Message[];                // pending delivery\n  hardSteering: boolean;           // context cutoff active\n  lastTokenTime: number;           // for health detection\n  spawnedAt: number | null;\n  unsubscribe: (() => void) | null; // session event unsub\n}\n```",
      "project": false,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T23:28:56.232Z",
      "updatedAt": "2026-03-15T23:46:33.160Z"
    },
    {
      "id": "4o3g4qf3",
      "categorySlug": "requirements",
      "title": "Unix Socket Protocol",
      "context": "When implementing the protocol adapter between manager extension and agent service",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change.\n\n## Unix Socket Protocol\n\nNewline-delimited JSON over Unix domain socket. Manager extension is client; agent service is server.\n\n- Socket path: from settings.json `service.socket_path` — see [n2j36dl7]\n- Each JSON message is one line terminated by `\\n`\n\n### Wire Format\n\n```json\n// Request (client → server)\n{ \"id\": \"req-1\", \"method\": \"agent.spawn\", \"params\": { \"name\": \"worker\" } }\n\n// Response\n{ \"id\": \"req-1\", \"result\": { \"status\": \"ok\" } }\n{ \"id\": \"req-1\", \"error\": { \"code\": \"ACL_DENIED\", \"message\": \"...\" } }\n\n// Event (server → client, pushed)\n{ \"event\": \"message.received\", \"data\": { \"from\": \"worker\", \"subject\": \"...\", \"body\": \"...\" } }\n```\n\n---\n\n## Commands\n\n#### Agent Lifecycle\n| Method | Params | Result |\n|--------|--------|--------|\n| `agent.spawn` | `{ name }` | `{ status: \"ok\" }` |\n| `agent.stop` | `{ name }` | `{ status: \"ok\" }` |\n| `agent.restart` | `{ name, prompt? }` | `{ status: \"ok\" }` |\n| `agent.compact` | `{ name }` | `{ status: \"ok\" }` |\n\n#### Agent State\n| Method | Params | Result |\n|--------|--------|--------|\n| `agent.status` | `{ name }` | `AgentState` — see [ik1yp2tc] |\n| `agent.list` | `{}` | `AgentSummary[]` |\n| `agent.config` | `{ name }` | `AgentConfig` (paths redacted — see [n2j36dl7]) |\n| `agent.peek` | `{ name, lines? }` | `{ output: string }` |\n\n#### Messaging\n| Method | Params | Result |\n|--------|--------|--------|\n| `message.send` | `{ from, to, subject, body, threadId?, replyTo?, priority?, delivery? }` | `{ messageId, threadId, status }` |\n| `message.list` | `{ agent?, threadId?, limit?, before? }` | `Message[]` |\n| `thread.list` | `{ agent? }` | `ThreadSummary[]` |\n\n#### Subscriptions (manager-only) — see [il2m3sl0]\n| Method | Params | Result |\n|--------|--------|--------|\n| `subscribe` | `{ filter, maxEvents }` | `{ subscriptionId }` |\n| `unsubscribe` | `{ subscriptionId }` | `{ status: \"ok\" }` |\n\n#### Trace — see [f5z68c4v]\n| Method | Params | Result |\n|--------|--------|--------|\n| `trace.query` | `{ agent?, type?, threadId?, after?, before?, limit? }` | `TraceEntry[]` |\n\n#### Configuration — see [n2j36dl7]\n| Method | Params | Result |\n|--------|--------|--------|\n| `config.reload` | `{}` | `{ status: \"ok\" }` |\n| `config.show` | `{}` | `Settings` (paths redacted) |\n| `acl.update` | `{ acl: AclRule[] }` | `{ status: \"ok\" }` |\n| `agent.register` | `{ name, path? }` | `{ status, scaffolded? }` |\n| `agent.unregister` | `{ name }` | `{ status: \"ok\" }` |\n\n#### Service\n| Method | Params | Result |\n|--------|--------|--------|\n| `service.ping` | `{}` | `{ status: \"ok\", uptime, agents: number }` |\n| `service.shutdown` | `{}` | `{ status: \"ok\" }` |\n\n---\n\n## Events (Server → Client)\n\nDelivered based on manager subscriptions — see [il2m3sl0]:\n\n| Event | Data |\n|-------|------|\n| `message.received` | `{ from, to, subject, body, threadId, messageId, priority }` |\n| `message.delivered` | `{ messageId, to }` |\n| `agent.state_changed` | `{ name, from: status, to: status }` |\n| `agent.health_changed` | `{ name, from: health, to: health }` |\n| `agent.context_warning` | `{ name, percent, level: \"polite\" \\| \"hard\" }` |\n| `agent.context_reset` | `{ name, summary, continueMessage }` |\n| `agent.spawned` | `{ name }` |\n| `agent.stopped` | `{ name, reason? }` |\n\n> Real-time tailing is a user-facing feature → epic [p09o4o3f]\n\n---\n\n## Error Codes\n\n| Code | Meaning |\n|------|---------|\n| `NOT_FOUND` | Agent not found |\n| `ALREADY_RUNNING` | Agent already spawned |\n| `NOT_RUNNING` | Agent is offline |\n| `ACL_DENIED` | ACL blocks this action |\n| `THREAD_LIMIT` | Thread at 100 message limit |\n| `INVALID_PARAMS` | Bad parameters |\n| `INTERNAL` | Unexpected server error |",
      "project": false,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T23:29:21.573Z",
      "updatedAt": "2026-03-15T23:46:56.927Z"
    },
    {
      "id": "ik1yp2tc",
      "categorySlug": "requirements",
      "title": "Agent State Machine, Health & Cutoff",
      "context": "When implementing agent state tracking, health monitoring, message delivery triggers, or context cutoff",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change.\n\n## Unattended Agent Principle\n\nAgents are fully unattended — no human watches them. The **only** external stimulus is `session.prompt()`, which starts a new turn. There is no polling, no timer, no subscription-based wake-up. If an agent needs to know something, the manager sends it a message.\n\n---\n\n## State Machine\n\n```\noffline ──spawn──→ online-idle ──agent_start──→ online-working\n                   online-idle ←──agent_end──── online-working\nonline-idle ──stop──→ offline\nonline-working ──stop──→ offline  (force stop)\n```\n\n### Transitions\n\n| From | To | Trigger | Notes |\n|------|----|---------|-------|\n| `offline` | `online-idle` | `spawnAgent()` | Session created, queued messages delivered |\n| `online-idle` | `online-working` | SDK `agent_start` | Agent began processing |\n| `online-working` | `online-idle` | SDK `agent_end` | Turn finished |\n| `online-idle` | `offline` | `stopAgent()` | Graceful dispose |\n| `online-working` | `offline` | `stopAgent()` | Force — abort + dispose |\n\nAll transitions traced — see [f5z68c4v].\n\n### What Wakes an Agent\n\n| Trigger | Mechanism | Example |\n|---------|-----------|---------|\n| Incoming message | `session.prompt()` | Manager sends task, another agent replies |\n| Cutoff warning | Queued as message → `session.prompt()` | Context at polite threshold |\n| Context reset | `session.prompt()` with continueMessage | After `newSession()` |\n| Initial spawn | `session.prompt()` with first task | Queued messages or initial prompt |\n\n**Nothing else wakes an agent.** Idle agents with no messages stay idle indefinitely (zero resource cost).\n\n---\n\n## Message Delivery by State\n\n| State | Delivery | Effect |\n|-------|----------|--------|\n| `online-idle` | **Immediate** via `session.prompt()` | **Starts a new turn** |\n| `online-working` + normal | Queued | Delivered after `agent_end`, triggers next turn |\n| `online-working` + important | Via `session.steer()` | Interrupts after current tool |\n| `offline` + persist | Queued | Delivered on next spawn |\n| `offline` + online-only | Dropped | Traced only |\n\n### Queue Drain Loop\n\n```\nOn agent_end:\n  1. Check context cutoff thresholds (polite/hard)\n  2. If messages in queue → deliver oldest via session.prompt() → new turn\n  3. If no messages → agent stays idle\n\nOn sendMessage (while agent is online-idle):\n  1. Deliver immediately via session.prompt() → new turn\n  2. Do NOT queue — go straight to delivery\n```\n\n---\n\n## Health Detection\n\nBased on **token gap timing** — interval between `text_delta` events during generation.\n\n| State | Condition | Meaning |\n|-------|-----------|---------|\n| `healthy` | Token gap < 10s | Normal speed |\n| `slow` | Token gap 10–60s | Rate limiting or complex thinking |\n| `stuck` | Token gap > 60s, or no events for 120s while working | May be hung |\n\n### Detection Logic\n\n```\nlastTokenTime = Date.now()  // reset on each text_delta\n\nEvery 5 seconds (while online-working):\n  gap = Date.now() - lastTokenTime\n  if gap > 60_000 → stuck\n  elif gap > 10_000 → slow\n  else → healthy\n\nAlso stuck if: online-working AND no session events for 120s\n```\n\n### Health Actions\n\n| Transition | Action |\n|------------|--------|\n| → `slow` | Trace + push `agent.health_changed` to manager |\n| → `stuck` | Trace + push `agent.health_changed`. Manager decides action. |\n| → `healthy` | Trace + push recovery event |\n\nService does NOT auto-remediate. Manager decides whether to restart, abort, or wait.\n\n---\n\n## Context Cutoff\n\nThresholds configurable per agent — see `cutoff_polite_pct` / `cutoff_hard_pct` in [n2j36dl7].\n\n### Polite Cutoff (default 70%)\n\n```\nOn agent_end:\n  pct = session.getContextUsage().percent\n  if pct >= polite_cutoff AND not already warned:\n    politeWarned = true\n    enqueue: \"Your context is at {pct}%. Wrap up and call context_handoff.\"\n    → delivered immediately via session.prompt() (agent is idle at agent_end)\n```\n\n### Hard Cutoff (default 90%)\n\n```\nOn agent_end:\n  if pct >= hard_cutoff: hardSteering = true\n\nOn tool_execution_end (while hardSteering):\n  session.steer(\"STOP. Context at {pct}%. Call context_handoff NOW.\")\n```\n\n### Handoff Execution\n\n```\nOn context_handoff tool call:\n  1. Capture summary + continueMessage\n  2. Trace: context_reset event — see [f5z68c4v]\n  3. Push event to manager subscriptions — see [il2m3sl0]\n  4. session.newSession()\n  5. Build message overview (unread, threads, handoff summary)\n  6. session.prompt(continueMessage + messageOverview) → new turn\n  7. Reset politeWarned, hardSteering\n```",
      "project": false,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T23:29:49.659Z",
      "updatedAt": "2026-03-15T23:47:32.342Z"
    },
    {
      "id": "f5z68c4v",
      "categorySlug": "requirements",
      "title": "Trace Log & State Recovery",
      "context": "When implementing the trace/audit system — JSONL format, entry types, query API, and crash recovery",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change.\n\n## Trace System\n\nAppend-only JSONL file at `service.trace_file` (default `.pi/agents/trace.jsonl` — see [n2j36dl7]).\n\n### Entry Base\n\n```typescript\ninterface TraceEntry {\n  id: string;              // unique (nanoid)\n  ts: string;              // ISO 8601 timestamp\n  type: TraceEntryType;    // discriminator\n  [key: string]: any;      // type-specific fields\n}\n```\n\n### Entry Types\n\n#### Messages\n```typescript\n{ type: \"message\", id, ts,\n  messageId, threadId, from, to, subject, body,\n  priority: \"normal\" | \"important\",\n  delivery: \"persist\" | \"online-only\",\n  replyTo?: string,\n  status: \"queued\" | \"delivered\" | \"dropped\"\n}\n```\n\n#### Message Status Updates\n```typescript\n{ type: \"message_status\", id, ts, messageId, status: \"delivered\" | \"read\" }\n```\n\n#### Agent State Transitions — see [ik1yp2tc]\n```typescript\n{ type: \"agent_state\", id, ts,\n  agent, from: Status, to: Status, reason?: string }\n```\n\n#### Agent Health Changes — see [ik1yp2tc]\n```typescript\n{ type: \"agent_health\", id, ts, agent, from: Health, to: Health }\n```\n\n#### Agent Lifecycle\n```typescript\n{ type: \"agent_spawned\", id, ts, agent, config: AgentConfig }\n{ type: \"agent_stopped\", id, ts, agent, reason?: string }\n{ type: \"agent_restarted\", id, ts, agent, prompt?: string }\n{ type: \"agent_compacted\", id, ts, agent }\n```\n\n#### Context Cutoff — see [ik1yp2tc]\n```typescript\n{ type: \"context_warning\", id, ts, agent, percent, level: \"polite\" | \"hard\" }\n{ type: \"context_reset\", id, ts, agent, percent, summary, continueMessage }\n```\n\n#### Service Lifecycle\n```typescript\n{ type: \"service_started\", id, ts }\n{ type: \"service_stopped\", id, ts }\n{ type: \"service_recovered\", id, ts, recoveredAgents: string[], pendingMessages: number }\n```\n\n### All Types\n\n```typescript\ntype TraceEntryType =\n  | \"message\" | \"message_status\"\n  | \"agent_state\" | \"agent_health\"\n  | \"agent_spawned\" | \"agent_stopped\" | \"agent_restarted\" | \"agent_compacted\"\n  | \"context_warning\" | \"context_reset\"\n  | \"service_started\" | \"service_stopped\" | \"service_recovered\";\n```\n\n---\n\n## Query API\n\nExposed via AgentManager (see [i5eisc9o]) and protocol (see [4o3g4qf3]):\n\n```typescript\nmanager.queryTrace(opts: {\n  agent?: string,\n  type?: TraceEntryType | TraceEntryType[],\n  threadId?: string,\n  after?: string,            // ISO timestamp\n  before?: string,           // ISO timestamp\n  limit?: number,            // default: 50, max: 500\n}): TraceEntry[]\n```\n\nMVP: sequential scan from end of file. No index.\n\n---\n\n## State Recovery\n\nOn service startup, if trace.jsonl exists:\n\n1. Scan all entries\n2. Rebuild message queues: `message` entries with `status: \"queued\"` lacking a corresponding `message_status: \"delivered\"`\n3. Rebuild thread metadata: message counts per thread, participants\n4. Do NOT re-spawn agents — they start offline. Manager decides what to spawn.\n5. Trace `service_recovered` entry\n\nRecovery is best-effort. Lost messages acceptable on crash.",
      "project": false,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T23:30:11.459Z",
      "updatedAt": "2026-03-15T23:47:53.800Z"
    },
    {
      "id": "il2m3sl0",
      "categorySlug": "requirements",
      "title": "Event Subscriptions (Manager-Only)",
      "context": "When implementing event subscriptions — manager-only, bounded delivery, loop prevention",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change.\n\n## Manager-Only Subscriptions\n\nEvent subscriptions exist **only for the manager**. Worker agents do NOT subscribe — they are simple message-driven workers. If an agent needs to know something, the manager sends it a message.\n\nThe manager receives events via the Unix socket (see [4o3g4qf3]) as pushed notifications.\n\n### Subscribe\n\nVia protocol command:\n\n```json\n{ \"method\": \"subscribe\", \"params\": { \"filter\": { ... }, \"maxEvents\": 1000 } }\n```\n\n```typescript\ninterface EventFilter {\n  types?: TraceEntryType[];    // see [f5z68c4v] for types\n  agent?: string;\n  threadId?: string;\n}\n```\n\n### Delivery\n\nWhen a trace entry matches a subscription's filter:\n1. Increment `deliveredCount`\n2. If `deliveredCount >= maxEvents` → auto-unsubscribe, final event has `subscription_expired: true`\n3. Push event to manager via socket\n\n### Auto-Subscriptions\n\nOn connect, the manager extension automatically creates:\n1. `message.received` where `to: \"manager\"` — maxEvents: 1000\n2. `agent.state_changed` — all agents — maxEvents: 1000\n3. `agent.health_changed` — all agents — maxEvents: 1000\n4. `agent.context_warning` — all agents — maxEvents: 1000\n\nRe-created on reconnect. Can be muted via `manager_unsubscribe` (see [5c5nibh6]).\n\n### Loop Prevention\n\nManager reactions can send messages → trigger agent work → produce more events.\n\n1. **Bounded delivery**: hard cap per subscription (`maxEvents`)\n2. **No auto-renew**: must explicitly re-subscribe\n3. **Max subscriptions**: 20 active\n\n### Lifecycle\n\n```\ncreated → active → expired (maxEvents reached)\n                 → cancelled (explicit unsubscribe)\n                 → cancelled (manager disconnects)\n```",
      "project": false,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T23:30:33.113Z",
      "updatedAt": "2026-03-15T23:48:08.545Z"
    },
    {
      "id": "a3m0qqbu",
      "categorySlug": "requirements",
      "title": "Reference File Structure",
      "context": "When understanding the full system layout — every file, who creates it, where it lives",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change.\n\n## Overview\n\nThe agent framework has three codebases and one auto-managed directory:\n\n1. **Agent Service** — standalone TypeScript process (npm package or local)\n2. **Manager Extension** — pi extension loaded into the manager's TUI session\n3. **Agent-side tools** — registered by the service via SDK `customTools` (no separate codebase)\n4. **Config directory** — `.pi/agents/` — auto-created with defaults on first extension load\n\n---\n\n## Auto-Scaffolding\n\nOn first `session_start`, the manager extension checks for `.pi/agents/` and creates everything missing with sensible defaults. The user never needs to manually create files — they just install the extension and go.\n\n```\nFirst run:\n1. .pi/agents/ doesn't exist → create it\n2. settings.json missing → create with defaults\n3. LOOP_INSTRUCTIONS.md missing → create with default template\n4. DEFERRED.md missing → create empty\n```\n\nSame for `agent_register` — creates agent folder + agent.json + SYSTEM.md with defaults.\n\n---\n\n## 1. Agent Service (`pi-agent-service/`)\n\nStandalone process. Started by the manager extension, runs independently.\n\n```\npi-agent-service/\n├── package.json\n├── src/\n│   ├── index.ts                    ← CLI entry point\n│   ├── manager.ts                  ← AgentManager class — see [i5eisc9o]\n│   ├── session-factory.ts          ← Creates SDK sessions per agent\n│   ├── router.ts                   ← MessageRouter: ACL, queue, deliver, threads\n│   ├── trace.ts                    ← TraceWriter: append-only JSONL — see [f5z68c4v]\n│   ├── recovery.ts                 ← StateRecovery: rebuild from trace\n│   ├── cutoff.ts                   ← CutoffMonitor: polite/hard per agent\n│   ├── health.ts                   ← HealthMonitor: token gap timing — see [ik1yp2tc]\n│   ├── subscriptions.ts            ← SubscriptionManager: manager-only — see [il2m3sl0]\n│   ├── settings.ts                 ← SettingsLoader: read + fs.watch()\n│   ├── agent-tools.ts              ← Builds 5 customTools per agent — see [0l0qkut7]\n│   ├── prompt-builder.ts           ← System prompt assembly — see [gzoe99o0]\n│   └── adapters/\n│       └── unix-socket.ts          ← NDJSON protocol adapter — see [4o3g4qf3]\n```\n\n---\n\n## 2. Manager Extension (`pi-agent-manager/`)\n\nLoaded by the user's pi TUI session.\n\n```\npi-agent-manager/\n├── package.json                    ← pi package manifest\n├── index.ts                        ← Hooks + auto-scaffolding — see [0osnjjl4]\n├── service-client.ts               ← Unix socket client\n├── scaffolding.ts                  ← Creates .pi/agents/ + defaults on first run\n├── tools/\n│   ├── agent-management.ts         ← agent_spawn, agent_stop, agent_restart\n│   ├── messaging.ts                ← agent_message, agent_messages, agent_threads\n│   ├── observability.ts            ← agent_status, agent_peek\n│   ├── configuration.ts            ← config_*, acl_*, agent_config_*, agent_system_prompt_*,\n│   │                                  agent_file_*, agent_create/delete, agent_extensions_*,\n│   │                                  monitor_config_*\n│   ├── loop.ts                     ← manager_monitor, manager_loop_next, manager_defer,\n│   │                                  loop_instructions_get, loop_instructions_update\n│   ├── subscriptions.ts            ← manager_subscribe, manager_unsubscribe, manager_subscriptions\n│   ├── debug.ts                    ← agent_trace, agent_history, agent_inspect, deferred_list,\n│   │                                  service_log, message_inspect, thread_inspect\n│   └── service.ts                  ← agent_service_start, agent_service_stop, agent_service_status\n├── modes.ts                        ← Mode switching, tool enable/disable — see [5c5nibh6]\n├── status-bar.ts                   ← TUI status bar\n└── state.ts                        ← Read/write manager-state.json\n```\n\n**How the user loads it:**\n\nOption A — installed as pi package: `pi install pi-agent-manager`\nOption B — local: `.pi/extensions/pi-agent-manager/`\nOption C — global: `~/.pi/agent/extensions/pi-agent-manager/`\n\n---\n\n## 3. Config Directory (`.pi/agents/`) — Auto-Created\n\nAll files auto-created with defaults on first extension load if missing.\n\n```\n.pi/agents/\n├── settings.json                   ← AUTO-CREATED with defaults, user edits — see [n2j36dl7]\n│                                     defaults, service, monitor, acl (empty), agents[]\n│\n├── manager-state.json              ← AUTO-MANAGED by manager extension\n│                                     { mode, loop state, subscriptions }\n│\n├── trace.jsonl                     ← AUTO-MANAGED by agent service\n│                                     Append-only audit log — see [f5z68c4v]\n│\n├── LOOP_INSTRUCTIONS.md            ← AUTO-CREATED with default template, manager updates\n│                                     Read each cycle, updated with learnings — see [8ikfn8hs]\n│\n├── DEFERRED.md                     ← AUTO-CREATED empty, manager appends\n│                                     Urgent notes to user — see [8ikfn8hs]\n│\n├── worker/                         ← Created by agent_register\n│   ├── agent.json                  ← AUTO-CREATED with defaults\n│   ├── SYSTEM.md                   ← AUTO-CREATED with template\n│   └── AGENTS.md                   ← AUTO-CREATED empty\n│\n└── reviewer/                       ← (agents can also be outside repo via absolute paths)\n    ├── agent.json\n    ├── SYSTEM.md\n    └── AGENTS.md\n```\n\n---\n\n## 4. Runtime Files\n\n```\n/tmp/\n├── pi-agent-service.sock           ← Unix socket (path from settings.json)\n├── pi-agent-service.pid            ← PID file\n└── pi-agent-service.log            ← Service log (if configured)\n```\n\n---\n\n## Relationship Diagram\n\n```\n┌─────────────────────────────┐\n│  User's pi TUI session      │\n│  ┌────────────────────────┐ │\n│  │ Manager Extension      │ │     Unix Socket\n│  │ (pi-agent-manager)     │──────────────────────┐\n│  │ - tools by mode        │ │                     │\n│  │ - status bar           │ │                     ▼\n│  │ - event listener       │ │     ┌──────────────────────────┐\n│  └────────────────────────┘ │     │  Agent Service            │\n│  + other extensions          │     │  (pi-agent-service)       │\n│  (PM, web-search, etc.)     │     │  - AgentManager           │\n└─────────────────────────────┘     │  - SDK sessions per agent │\n                                    │  - MessageRouter          │\n                                    │  - TraceWriter            │\n                                    │  - HealthMonitor          │\n                                    └──────────────────────────┘\n                                         │  │  │\n                                    SDK sessions (in-process)\n                                         │  │  │\n                                    ┌────┘  │  └────┐\n                                    ▼       ▼       ▼\n                                 worker  reviewer  researcher\n                                 (SDK)   (SDK)     (SDK)\n```",
      "project": false,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T23:49:15.243Z",
      "updatedAt": "2026-03-16T00:12:23.393Z"
    },
    {
      "id": "0osnjjl4",
      "categorySlug": "requirements",
      "title": "Extension Hooks & Lifecycle",
      "context": "When implementing the manager extension or agent service — all registered hooks, events, and their responsibilities",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change.\n\n## Manager Extension Hooks\n\nRegistered in `index.ts` of the manager extension (pi-agent-manager). Uses the pi Extension API (`pi.on()`, `pi.registerTool()`, etc.).\n\n### Lifecycle Hooks\n\n#### `session_start`\n**When:** Extension loads (pi starts or `/reload`)\n**Does:**\n1. Read `manager-state.json` → restore current mode\n2. Try connect to agent service socket\n3. If service not running → auto-start it (`child_process.spawn`)\n4. Wait for socket connection (with timeout)\n5. Flush queued manager messages (from service)\n6. Re-create auto-subscriptions (messages, state changes, health, context warnings)\n7. Re-activate persisted custom subscriptions\n8. Register all tools (filtered by current mode — see [5c5nibh6])\n9. Set up status bar via `ctx.ui.setStatus()`\n10. If restoring from context reset → inject message overview as user message\n11. If mode=loop → resume loop at persisted cycle count — see [8ikfn8hs]\n\n#### `session_shutdown`\n**When:** pi is exiting\n**Does:**\n1. Close socket connection (service stays running)\n2. Save manager-state.json (current mode, loop state, custom subscriptions)\n\n#### `before_agent_start`\n**When:** Before each LLM turn — **rebuilds system prompt every turn**\n**Does:**\n1. Re-read manager's SYSTEM.md from disk (live changes take effect)\n2. Inject mode-specific instructions via `addSystemPrompt()` — see [5c5nibh6]\n3. Inject address book (ACL-filtered agent list + briefs) — see [gzoe99o0]\n4. If in loop mode → inject cycle count and monitor state\n\n#### `agent_end` (loop keep-alive)\n**When:** Manager's LLM turn ends — **only active in loop mode**\n**Does:**\nIf loop mode is active and `manager_loop_next` was NOT called this turn:\n- Inject via `pi.sendUserMessage()`:\n  \"⚠️ You are in LOOP mode but did not call manager_loop_next.\n   Either continue working or call manager_loop_next to start the countdown.\"\n- This triggers a new turn, preventing the manager from going idle mid-cycle\n\nIf `manager_loop_next` WAS called:\n- Countdown is running, manager is allowed to be idle\n- Countdown completion or subscribed event will wake it\n\nFull loop lifecycle in [8ikfn8hs].\n\n### Event Listeners (on socket)\n\nListening on the Unix socket connection to the service:\n\n#### `message.received` (socket event)\n**When:** Message arrives for manager\n**Does:** `pi.sendUserMessage()` — injects message into conversation, triggers a turn\n\n#### `agent.state_changed` (socket event)\n**When:** Any agent changes state\n**Does:** If agent goes stuck/stopped → `pi.sendUserMessage()` to alert manager\n\n#### `agent.health_changed` (socket event)\n**When:** Agent health degrades\n**Does:** `pi.sendUserMessage()` with health alert\n\n#### `agent.context_warning` (socket event)\n**When:** Agent hits cutoff threshold\n**Does:** `pi.sendUserMessage()` to inform manager\n\n### Tool Registration\n\nAll tools registered via `pi.registerTool()` on `session_start`. Mode switching dynamically enables/disables tools when `manager_mode` is called.\n\nEach tool is a thin wrapper that:\n1. Validates params\n2. Sends protocol command to service via socket\n3. Returns formatted response\n\nSee [5c5nibh6] for the full tool inventory by mode.\n\n---\n\n## Agent Service — SDK Session Hooks\n\nNOT pi extension hooks. These are `session.subscribe()` callbacks registered by the AgentManager per agent.\n\n### System Prompt Rebuild\n\nThe `systemPromptOverride` passed to `createAgentSession()` is a **function**, called before every turn. On each call it:\n1. Re-reads agent's SYSTEM.md from disk (live changes)\n2. Includes coding guidelines if `coding_tools=true`\n3. Injects agent's own brief\n4. Builds address book from in-memory ACL + agent briefs (updated by settings watcher)\n5. Appends unattended instructions\n\nFull assembly order in [gzoe99o0].\n\n### Per-Agent Session Subscription\n\nRegistered in `manager.spawnAgent()`:\n\n```typescript\nconst unsub = session.subscribe((event) => { ... });\n```\n\n#### `agent_start`\n**When:** Agent begins processing a prompt\n**Does:**\n1. Transition state: `online-idle` → `online-working`\n2. Trace `agent_state` entry\n3. Push `agent.state_changed` to manager subscriptions\n4. Start health monitoring timer\n\n#### `agent_end`\n**When:** Agent finishes a turn (all tool calls complete)\n**Does:**\n1. Transition state: `online-working` → `online-idle`\n2. Trace `agent_state` entry\n3. Push `agent.state_changed` to manager subscriptions\n4. Stop health monitoring timer\n5. Check context cutoff:\n   - If `pct >= hard_cutoff` → set `hardSteering = true`\n   - Elif `pct >= polite_cutoff` and not yet warned → enqueue polite warning\n6. Check message queue:\n   - If messages waiting → deliver oldest via `session.prompt()` → triggers new turn\n   - If no messages → agent stays idle\n\n#### `tool_execution_end`\n**When:** A single tool call completes (agent may make more)\n**Does:**\n1. If `hardSteering` active → `session.steer(\"STOP. Call context_handoff NOW.\")`\n\n#### `text_delta` (via `message_update`)\n**When:** Token streamed from LLM\n**Does:**\n1. Update `lastTokenTime` for health detection\n2. (Future: feed to tailing system — epic [p09o4o3f])\n\n#### `auto_compaction_start`\n**When:** SDK auto-compaction triggers\n**Does:** Should NOT happen — compaction must be disabled. Log warning.\n\n### Agent Custom Tool Hooks\n\nTool `execute` handlers registered via `customTools`. See [0l0qkut7] for params.\n\n#### `send_message` execute\n1. ACL check via router (in-memory, updated by settings watcher)\n2. Create message entry, assign to thread\n3. Trace `message` entry\n4. Route to recipient (queue or deliver — see [ik1yp2tc])\n5. Return confirmation string\n\n#### `context_handoff` execute\n1. Capture `summary` + `continueMessage`\n2. Trace `context_reset` entry\n3. Push event to manager subscriptions\n4. `session.newSession()`\n5. Build message overview (unread, threads, handoff summary)\n6. `session.prompt(continueMessage + overview)` → new turn in fresh context\n7. Reset `politeWarned`, `hardSteering`\n\n#### `list_messages` execute\n1. Query router for agent's messages (filtered by thread/limit)\n2. Format and return\n\n#### `list_threads` execute\n1. Query router for agent's threads\n2. Format and return\n\n#### `check_status` execute\n1. Read `session.getContextUsage()`\n2. Gather stats from ManagedAgent state\n3. Format and return\n\n---\n\n## Health Monitor (internal timer)\n\nNot a hook — a `setInterval` managed per agent.\n\n**Started:** on `agent_start` event\n**Stopped:** on `agent_end` event or `stopAgent()`\n**Interval:** every 5 seconds\n\n**Each tick:**\n```\ngap = Date.now() - lastTokenTime\nnewHealth = gap > 60s ? \"stuck\" : gap > 10s ? \"slow\" : \"healthy\"\nif newHealth !== currentHealth:\n  trace agent_health entry\n  push agent.health_changed to manager subscriptions\n  currentHealth = newHealth\n```\n\nSee [ik1yp2tc] for thresholds and details.\n\n---\n\n## Settings File Watcher\n\nThe agent service watches `settings.json` with `fs.watch()`. On change:\n1. Re-read and parse\n2. Update in-memory ACL (next `send_message` ACL check + next system prompt rebuild reflect it)\n3. Update in-memory agent configs (affects next spawn)\n4. Do NOT restart running agents — changes apply on next turn / next spawn\n\nSee [gzoe99o0] for how this feeds into system prompt assembly.\n\n---\n\n## Summary: What Registers What\n\n| Component | Method | What |\n|-----------|--------|------|\n| Manager Extension | `pi.on(\"session_start\")` | Init, connect, restore mode/loop |\n| Manager Extension | `pi.on(\"session_shutdown\")` | Save state, close socket |\n| Manager Extension | `pi.on(\"before_agent_start\")` | Rebuild system prompt every turn |\n| Manager Extension | `pi.on(\"agent_end\")` | Loop keep-alive (loop mode only) — see [8ikfn8hs] |\n| Manager Extension | `pi.registerTool()` | ~40 tools, filtered by mode — see [5c5nibh6] |\n| Manager Extension | `ctx.ui.setStatus()` | Status bar |\n| Manager Extension | Socket listener | Event handlers for pushed events |\n| Agent Service | `systemPromptOverride` (fn) | Rebuilt per-turn — see [gzoe99o0] |\n| Agent Service | `session.subscribe()` | Per-agent: `agent_start`, `agent_end`, `tool_execution_end`, `text_delta` |\n| Agent Service | `customTools` | 5 agent tools — see [0l0qkut7] |\n| Agent Service | `setInterval` | Health monitor timer per agent |\n| Agent Service | `fs.watch()` | Settings file watcher for live ACL/config updates |",
      "project": false,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-15T23:50:03.566Z",
      "updatedAt": "2026-03-16T00:03:04.633Z"
    },
    {
      "id": "8ikfn8hs",
      "categorySlug": "requirements",
      "title": "Manager Loop Mode",
      "context": "When implementing the manager's loop/monitor mode — cycle lifecycle, agent-stop prevention, countdown, wake-on-event",
      "body": "> **📋 Live document.** Update when implementation drifts from spec or when decisions change.\n\n## Overview\n\nLoop mode makes the manager run **unattended** in repeating oversight cycles. Each cycle: check status, act on issues, defer what can't be solved, sleep, repeat. The loop is bounded by `max_turns` to prevent runaway costs.\n\n---\n\n## Loop Lifecycle\n\n### Starting the Loop\n\nUser says \"start monitoring\" or similar → manager calls `manager_monitor action:\"start\"`.\n\n1. Set mode to `loop` (persists to manager-state.json)\n2. Initialize cycle counter: `cycle = 0`\n3. Inject loop system prompt instructions (see [5c5nibh6])\n4. Begin first cycle immediately\n\n### Single Cycle Flow\n\n```\n1. Increment cycle counter: cycle++\n2. Read instructions via loop_instructions_get (re-read each cycle — live editable)\n3. Check subscribed events (any fired since last cycle?)\n4. Check all agent statuses (health, context %, state)\n5. Check messages (any unread from agents?)\n6. Act on findings:\n   - Stuck agent? → restart or message it\n   - Agent finished? → assign next task\n   - Error? → investigate and fix or defer\n   - Context warning? → trigger handoff\n7. If issue unsolvable → call manager_defer (critical note to user)\n8. Call manager_loop_next → starts countdown to next cycle\n```\n\n### Countdown & Wake-on-Event\n\nAfter `manager_loop_next`:\n1. Start countdown timer (`interval_seconds`, default 300s)\n2. Display countdown in status bar: `🔄 LOOP 3/10 | ⏱️ 4:23`\n3. **If a subscribed event fires during countdown:**\n   - Cancel remaining countdown\n   - Inject the event as a user message via `pi.sendUserMessage()`\n   - Triggers next cycle immediately\n4. **If countdown completes normally:**\n   - Inject \"Loop cycle {N}: Begin monitoring check.\" as user message\n   - Triggers next cycle\n\n### Loop Termination\n\n| Condition | What happens |\n|-----------|-------------|\n| `cycle >= max_turns` | Inject: \"Loop reached max {max_turns} cycles. Stopping.\" Switch to operate mode. |\n| `manager_monitor action:\"stop\"` | User explicitly stops. Switch to operate mode. |\n| `manager_mode` called with non-loop mode | Implicit stop. |\n| Context cutoff (manager itself) | Manager calls `loop_instructions_update` with learnings → handoff → tmux restart → loop resumes. |\n\n---\n\n## Preventing Agent Stop (Manager Keep-Alive)\n\nThe manager extension hooks `agent_end` in loop mode:\n\n```\nOn agent_end (while loop mode active):\n  if manager did NOT call manager_loop_next this turn:\n    → inject via pi.sendUserMessage():\n      \"⚠️ You are in LOOP mode but did not call manager_loop_next.\n       Either continue working or call manager_loop_next to start \n       the countdown to the next cycle.\"\n    → triggers a new turn, preventing idle\n\n  if manager DID call manager_loop_next:\n    → countdown is running, manager is allowed to be idle\n    → countdown or event will wake it\n```\n\n---\n\n## Cycle Counter & State\n\nPersisted in `manager-state.json`:\n\n```json\n{\n  \"mode\": \"loop\",\n  \"loop\": {\n    \"cycle\": 3,\n    \"max_turns\": 10,\n    \"started_at\": \"2025-03-15T10:00:00Z\",\n    \"countdown_active\": true,\n    \"countdown_until\": \"2025-03-15T10:05:00Z\"\n  }\n}\n```\n\nOn context restart:\n1. Read manager-state.json\n2. If `mode === \"loop\"` → resume at current cycle count\n3. Inject: \"Context restarted. Resuming loop at cycle {cycle}/{max_turns}.\"\n\n---\n\n## LOOP_INSTRUCTIONS.md\n\nLocation: `settings.monitor.instructions_file` (default `.pi/agents/LOOP_INSTRUCTIONS.md`).\n\nAccessed via tools only — manager doesn't know the file path:\n- **`loop_instructions_get`** — read current instructions\n- **`loop_instructions_update`** — update with learnings\n\nRead at the start of each cycle. Manager updates it when it learns something new.\n\n### When to Update\n\n| Trigger | What to write |\n|---------|---------------|\n| Learned a new pattern | Add to policies (e.g. \"agents get stuck after large file edits → restart\") |\n| Found something irrelevant | Remove from check order |\n| **Context cutoff** | **Must call `loop_instructions_update` before `context_handoff`** — capture all learnings so fresh context has them |\n\nThe loop injected instructions (see [5c5nibh6]) explicitly tell the manager:\n> \"Before calling context_handoff, always call loop_instructions_update to save your learnings.\"\n\n### Initial Content (user-written)\n\n```markdown\n# Loop Instructions\n\n## Check Order\n1. Review all agent statuses — look for stuck or slow agents\n2. Check for unread messages from agents\n3. Check if any agents are idle that should be working\n\n## Policies\n- If an agent is stuck for >5 min, restart it\n- If a worker finishes its task, check the issue tracker for next work\n- If reviewer finds critical issues, message worker immediately\n- If costs exceed $5 total, stop all agents and defer to human\n\n## Do NOT\n- Start new epics without human approval — defer these\n- Change agent system prompts\n- Modify ACL\n```\n\n### Parallel: Agent Learnings\n\n| Role | Read each turn/cycle | Self-written learnings |\n|------|----------------------|------------------------|\n| Manager | LOOP_INSTRUCTIONS.md via `loop_instructions_get/update` | Same file — updates in place |\n| Agent | SYSTEM.md (user-written, read-only to agent) | AGENTS.md (agent writes learnings) |\n\n---\n\n## Deferred Items\n\nWhen the manager **cannot solve something autonomously**, it appends an urgent note for the human to `.pi/agents/DEFERRED.md`.\n\n### `manager_defer`\n```\nParameters:\n  title: string       — short description of the issue\n  body: string        — full context, what was tried, what's needed from the human\n```\n\nThe tool simply appends a markdown section to DEFERRED.md:\n\n```markdown\n## ⚠️ {title}\n*Deferred at {timestamp} — Loop cycle {cycle}*\n\n{body}\n\n---\n```\n\n### Example DEFERRED.md\n\n```markdown\n## ⚠️ Need human decision on database migration strategy\n*Deferred at 2025-03-15 10:08 — Loop cycle 3*\n\nWorker asked about breaking schema change. Two options discussed in thread t-abc123.\nNeed human to pick approach before work can continue.\n\n---\n\n## ⚠️ Cost threshold approaching\n*Deferred at 2025-03-15 10:15 — Loop cycle 5*\n\nTotal spend at $4.50, threshold is $5. Stopped all agents.\nResume with higher threshold or change approach.\n\n---\n```\n\nViewable via `deferred_list` (reads and returns DEFERRED.md contents). User resolves by telling the manager what to do.\n\n---\n\n## Loop Mode Tools\n\n| Tool | Purpose |\n|------|---------|\n| `manager_monitor` | `action: \"start\" \\| \"stop\"` — start/stop the loop |\n| `manager_loop_next` | Signal cycle done, start countdown |\n| `manager_defer` | Append urgent note to DEFERRED.md (title + body) |\n| `loop_instructions_get` | Read current loop instructions |\n| `loop_instructions_update` | Update instructions with learnings (must call before context_handoff) |\n\nPlus all operate mode tools. See [5c5nibh6] for complete inventory.",
      "project": false,
      "sources": [],
      "linkedEpicIds": [
        "iduwan11"
      ],
      "linkedIssueIds": [],
      "createdAt": "2026-03-16T00:00:58.249Z",
      "updatedAt": "2026-03-16T00:11:29.800Z"
    }
  ]
}
